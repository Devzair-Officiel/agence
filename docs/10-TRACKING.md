# Suivi, décisions et changements

> État vivant du projet : tâches, décisions et historique.

## 24. Registre de suivi

| ID | Phase | Tâche | Priorité | Statut | Preuve attendue | Prochaine action |
|---|---|---|---|---|---|---|
| DEV-001 | 0 | Confirmer les services du MVP | Haute | À faire | Liste validée | Cadrage éditorial |
| DEV-002 | 0 | Confirmer la zone desservie | Haute | À faire | Information écrite | Valider les pages locales |
| DEV-003 | 0 | Inventorier les réalisations publiables | Haute | À faire | Dossiers et autorisations | Préparer les études de cas |
| DEV-004 | 1 | Créer Dockerfile.dev | Haute | Terminé | `apps/web/Dockerfile.dev` (Node 24 Alpine, `npm ci` au build) + `.dockerignore` | Écrire un Dockerfile de production en Phase 12 |
| DEV-005 | 1 | Créer compose.yaml | Haute | Terminé | Site accessible sur `http://localhost:3001` (mapping 3001→3000 conteneur), `docker compose config` OK, volumes `web_node_modules` et `web_nuxt_cache` | Ajouter le service `api` (Symfony) en Phase 8 |
| DEV-006 | 1 | Initialiser Git à la racine | Haute | En cours | `.git` racine présent, `.gitignore` posé, un seul `.git` dans l’arbre | Créer le premier commit propre (humain) |
| DEV-007 | 2 | Installer ESLint et le typecheck | Haute | Terminé | `@nuxt/eslint` en devDep, `eslint.config.mjs` en Flat Config (ignore `.nuxt`/`.output`, refuse `no-explicit-any`), `npm run lint` et `npm run typecheck` verts | Étendre les règles au besoin sans surcharger |
| DEV-008 | 2 | Créer l’architecture Nuxt | Haute | Terminé | `app/{assets,components,pages,composables,layouts}` en place, `error.vue` et `app.vue` présents | Créer `server/` et `shared/` quand un besoin réel apparaît (Phase 8+) |
| DEV-009 | 4 | Créer `usePageSeo` | Haute | Terminé | `app/composables/usePageSeo.ts` + utilitaires purs `app/utils/site-url.ts` et `app/utils/canonical.ts` ; `test/unit/composables/usePageSeo.spec.ts` (7 cas indexable/noindex/robots explicite/localhost) et `test/unit/utils/canonical.spec.ts` verts | Migrer les pages Phase 5 au fur et à mesure |
| DEV-010 | 4 | Configurer robots, sitemap et Schema.org | Haute | Terminé | `@nuxtjs/robots` v5.7 + `@nuxtjs/sitemap` v7.6 installés ; `useSiteSchema` injecte Organization + WebSite dans le layout par défaut ; `test/e2e/seo.spec.ts` vert. Curl : `X-Robots-Tag: noindex, nofollow` sur `/`, `robots.txt` : `User-agent: * / Disallow: /`, sitemap XML absolu avec une seule route `/` depuis la clôture Phase 5D | Réévaluer `nuxt-schema-org`, `nuxt-link-checker`, `nuxt-seo-utils` en Phase 9 |
| DEV-011 | 3 | Poser les tokens CSS (couleurs, typos, espacements, radius, ombres, animations, z-index) | Haute | Terminé | `apps/web/app/assets/css/tokens.css` + `global.css` + `animations.css` chargés par Nuxt | Utiliser les tokens dans les pages futures |
| DEV-012 | 3 | Créer les composants de base (BaseContainer, BaseEyebrow, BaseButton, BaseLink) | Haute | Terminé | Composants + tests Vitest (7 cas BaseButton) verts | Étendre au besoin sans surconfigurer |
| DEV-013 | 3 | Créer le header, la nav mobile accessible et le footer | Haute | Terminé | `SiteHeader`, `MobileNavigation` (Teleport, aria-modal, focus trap, Escape, scroll lock, restauration du focus), `SiteFooter`, tests Vitest (10 cas) verts | Ajouter les tests clavier/contrastes et zoom (Phase 3) |
| DEV-014 | 3-5D | Ajouter la page interne `/design-preview` (noindex) | Basse | Terminé | Page rendue en SSR avec `<meta name="robots" content="noindex, nofollow">` | **Retirée en Phase 5D** : coverage utile migrée dans `test/e2e/home-structure.spec.ts`, sitemap n'excluant plus rien, `/design-preview` renvoie 404 (vérifié par `seo.spec.ts`) |
| DEV-015 | 2 | Câbler Vitest 3 + @vue/test-utils + happy-dom, ajouter `vue-tsc` et `typescript` en devDeps, scripts `typecheck` et `test` | Haute | Terminé | `docker compose exec web npm run test` (20 verts), `typecheck` OK, `build` OK | Ajouter ESLint et Playwright plus tard |
| DEV-016 | 1-2 | Clôturer les fondations techniques (Docker reproductible, ESLint Flat Config, Playwright prep, CI GitHub Actions, `@nuxt/fonts`, README, `.env.example`) | Haute | Terminé | `docker compose build` OK, `npm run quality` vert (lint + typecheck + tests 20/20 + build), workflow `.github/workflows/web-quality.yml` en place, smoke test Playwright écrit | Enchaîner sur la Phase 5 (hero + graphe SVG) |
| DEV-017 | 2 | Corriger la régression vue-router R0004 (accueil `/` manquant, `<a>` internes vers routes inexistantes, `@font-face` sur fichiers absents) | Haute | Terminé | Console navigateur propre sur `/design-preview`, page `/` placeholder noindex, prop `external` sur `BaseButton`/`BaseLink` pour les liens vers pages non encore livrées | Retirer `external` au fur et à mesure que les pages sont créées |
| DEV-018 | 3 | Écrire la suite E2E Playwright de clôture Phase 3 (design-preview, mobile-navigation, keyboard-navigation, responsive 390/768/1440, reduced-motion, Axe WCAG 2.2 AA) | Haute | Terminé | 28/28 tests verts dans `mcr.microsoft.com/playwright:v1.62.1-noble` ciblant le conteneur `web` sur `localhost:3001` ; aucune violation Axe `serious`/`critical` sur `/design-preview` | Étendre le scan Axe aux pages marketing en Phase 5 |
| DEV-019 | 3 | Corriger les défauts démontrés par la suite Axe/Playwright | Haute | Terminé | `SiteFooter` : `#5e6b78` → `var(--color-cream-subtle)` (~6:1 sur `--color-ink`) ; `design-preview` : swatch Devzair blue en `color: #000` (≥5.9:1) et `code` en `opacity: 1` | Aucune |
| DEV-020 | 3 | Ajouter le job `e2e` GitHub Actions dans l'image officielle Playwright et exposer `PLAYWRIGHT_BASE_URL` | Haute | Terminé | `.github/workflows/web-quality.yml` : job `e2e` parallèle au job `quality`, image `mcr.microsoft.com/playwright:v1.62.1-noble`, `webServer` conditionnel dans `playwright.config.ts` | Ajouter un job équivalent pour Symfony (Phase 8) |
| DEV-021 | 4 | Étendre `runtimeConfig.public` avec `siteUrl` et `siteIndexable`, mettre à jour `.env.example` et `compose.yaml` | Haute | Terminé | `NUXT_PUBLIC_SITE_URL` et `NUXT_PUBLIC_SITE_INDEXABLE` (défaut `false`) mirroirés dans `site.url` et `site.indexable` ; `.env.example` documente le défaut « safe » | Forcer `NUXT_PUBLIC_SITE_INDEXABLE=true` uniquement en production (Phase 12) |
| DEV-022 | 4 | Injecter Schema.org Organization + WebSite au niveau du layout | Haute | Terminé | `app/composables/useSiteSchema.ts` appelé une fois dans `app/layouts/default.vue` ; JSON-LD présent dans le HTML SSR (curl `/`) ; aucun champ fictif (`aggregateRating`, `address` absents) | Étendre au type `Article` en Phase 9 (à ce moment réévaluer `nuxt-schema-org`) |
| DEV-023 | 4 | Documenter les modules SEO retenus et ceux différés | Moyenne | Terminé | `docs/adr/ADR-004-modules-seo.md` (Option 2 « à la carte », @nuxtjs/robots + @nuxtjs/sitemap seuls, autres modules différés avec justification) | Rouvrir l'ADR à l'ouverture de la Phase 9 |
| DEV-024 | 4 | Créer l'emplacement de l'image OG par défaut sans publier de fichier fictif | Moyenne | Terminé | `apps/web/public/og/.gitkeep` avec instructions de drop-in ; `site.defaultOgImage = null` ; `usePageSeo` n'émet pas de `og:image` tant qu'aucune image n'est fournie | Livrer l'image OG statique validée en Phase 5 |
| DEV-025 | 5A | Livrer la page d'accueil (hero + graphe SVG des cinq pôles) avec SEO réel et pré-rendu | Haute | Terminé | `app/config/expertise-pillars.ts` (contrat typé, 5 pôles), `HomeEcosystemGraph.vue` (SVG accessible role="img" + `<title>`/`<desc>` liés par `aria-labelledby` via `useId()`, animation signature CSS ~1,4 s non bouclée, défense en profondeur `prefers-reduced-motion`), `HomeHero.vue` (eyebrow, H1, CTA vers `#contact`/`#realisations`, réassurance), `pages/index.vue` recâblée sur `usePageSeo({ type: "website" })`, `routeRules['/'] = { prerender: true }`. Curl SSR : `<html lang="fr">`, titre `Agence digitale pour sites web, applications et visibilité \| Devzair`, description validée, canonical `/`, `og:type website`, `twitter:card summary` (pas de logo dispo). Suite étendue : 69 tests unitaires (+17), 51 E2E (+13) verts. Lint, typecheck, build OK | Enchaîner Phase 5B (sections « Le constat », « Réponse globale », « Cinq pôles détaillés ») |
| DEV-026 | 5A | Image Open Graph statique `og/devzair-home.png` (1200×630) pour l'accueil | Moyenne | Bloqué | Aucun logo Devzair présent dans le dépôt (`apps/web/public/` ne contient que `favicon.ico` et `og/.gitkeep`). `defaultOgImage` reste `null`, `usePageSeo` n'émet pas de `og:image` et Twitter retombe sur `summary` (comportement attendu) | Obtenir le logo Devzair (SVG ou PNG haute résolution) auprès du client, puis générer une image 1200×630 alignée sur la charte |
| DEV-027 | 5B | Livrer les trois sections primaires de l'accueil : « Le constat », « La réponse Devzair », « Nos expertises · 5 pôles » | Haute | Terminé | `expertise-pillars.ts` étendu (`longDescription` + `services` + variante `accent`), 3 composants créés (`HomeProblems.vue`, `HomeConnectedApproach.vue`, `HomeExpertisePillars.vue`), `pages/index.vue` orchestre 4 sections. Carrousel mobile CSS-natif (scroll-snap), grille desktop asymétrique 3×2 (Concevoir spans 2 rows, Faire évoluer petrol). Ancre `#expertises` active. 90 tests unitaires (+21), 72 E2E (+21) verts. Curl SSR : 3 H2, 5 `home-problems__number`, 5 `home-approach__step-title`, 5 `home-pillars__card-title`, `id="expertises"` et « Faites défiler » présents. Build : 2.79 MB / 725 kB gzip (+40 KB / +11 KB gzip vs 5A) | Enchaîner Phase 5C (réalisation-témoin, méthode, pourquoi Devzair, CTA final) |
| DEV-028 | 5B | Corriger deux défauts de contraste WCAG démontrés par Axe sur les nouvelles sections | Haute | Terminé | `HomeConnectedApproach.step-index` : Devzair blue #2e86d9 sur navy-elevated #141e2c donnait 4.41:1 (petit texte gras → seuil 4.5:1). Passé sur cream (~13:1). `HomeExpertisePillars` carte accent : Devzair blue sur petrol #0c5b57 donnait 2.08:1. Passé sur cream (~8.8:1) pour index/tag, cream-muted pour la puce ::before | Aucune |
| DEV-029 | 5C | Livrer les trois sections secondaires de l'accueil : « Réalisations » (état honnête), « Notre méthode » (6 étapes), « Pourquoi Devzair » (5 promesses) | Haute | Terminé | 2 configs typées créées (`project-process.ts` 6 étapes, `trust-promises.ts` 5 promesses, texte verbatim brief 5C). 3 composants créés : `HomeFeaturedCaseStudy.vue` (ancre `#realisations`, variante honest-state — aucun faux client), `HomeProcess.vue` (`<ol>` 6 étapes, timeline verticale mobile via `::before`/`::after`, 2 col ≥640px, 3×2 ≥1024px), `HomeTrust.vue` (`<ul>` 5 promesses, 1 col mobile, 2 col+row-span ≥640px, grille 3+2 centrée ≥1024px). `pages/index.vue` orchestre 7 sections. `navigation.ts` : lien header « Réalisations » recablé de `/realisations` (route absente) vers `/#realisations` (ancre active). CTA hero `#realisations` opérationnel sans modification. 120 tests unitaires (+30 : 5+6+7+6+6), 97 E2E (+25 dans `home-sections-secondary.spec.ts`) verts. Curl SSR : 18 signatures éditoriales exactes présentes (H2, honest-state, 6 labels méthode, 5 labels promesses, `id="realisations"`, `/#realisations`). Build : 2.83 MB / 733 kB gzip (+45 KB / +8 KB gzip vs 5B) | Enchaîner Phase 5D (CTA final, ancre de contact temporaire, harmonisation éditoriale, suppression de `/design-preview`) |
| DEV-030 | 5C | Documenter l'absence de projet publiable et la variante « état honnête » retenue pour la section Réalisations | Haute | Terminé | DEV-003 « Inventorier les réalisations publiables » reste `À faire`. Aucune étude de cas Devzair ne satisfait actuellement les exigences de `docs/01-CONTENT.md §7.6` (client autorisé, résultats mesurables, méthode/période de mesure, visuels autorisés). `HomeFeaturedCaseStudy.vue` publie donc eyebrow `Études de cas en préparation` + titre `Nos réalisations détaillées seront bientôt disponibles.` + paragraphe explicatif. Aucun bouton, aucun lien, aucune image. Le jour où DEV-003 est livré, remplacer le bloc `home-case__placeholder` par la variante détaillée sans changer l'ancre ni le titre externe. Cf. DEC-035 | Livrer un premier cas réel dès qu'un projet Devzair aura obtenu autorisation client + résultats vérifiables |
| DEV-031 | 5D | Livrer la section finale CTA `#contact` et clore l'accueil publique (8 sections) | Haute | Terminé | `HomeCallToAction.vue` (section ancrée `#contact`, verbatim `Parlons de votre projet` / `Construisons une présence digitale à la hauteur de votre entreprise.` / paragraphe explicatif). Stratégie de contact conditionnelle (mailto BaseButton si `site.contact.email` défini, sinon rien). Notice preprod discrète visible uniquement si `!contact.email && siteIndexable === false` (source unique `useRuntimeConfig().public.siteIndexable`, jamais `process.env`). `pages/index.vue` orchestre les 8 sections. `primaryCta.to = "/#contact"` (source unique header desktop / footer / menu mobile) ; header nav « Expertises » recablé `/expertises` → `/#expertises`. Tests unitaires : 130 verts (+10 `HomeCallToAction.spec.ts`). Tests E2E : `home-cta-final.spec.ts` (+13) et `home-structure.spec.ts` (+5) ; `home-sections-secondary.spec.ts` étendu à 8 sections. Curl SSR : `id="contact"`, eyebrow, H2, paragraphe et notice preprod visibles avant hydratation | Livrer le formulaire réel (validation, anti-spam, e-mail transactionnel) en Phase 6 |
| DEV-032 | 5D | Supprimer `/design-preview` et migrer sa couverture E2E utile vers `/` | Haute | Terminé | `apps/web/app/pages/design-preview.vue` et `test/e2e/design-preview.spec.ts` supprimés. `test/e2e/home-structure.spec.ts` créé (5 tests : console/router propre, single H1, skip link `#main-content` sur vrai `<main>`, header + footer présents, aucune coordonnée fictive). Sitemap : `exclude` retiré (route inexistante). `test/e2e/seo.spec.ts` vérifie désormais que `/design-preview` renvoie 404. `beforeEach`/`goto('/design-preview')` migrés vers `/` dans `accessibility.spec.ts`, `keyboard-navigation.spec.ts`, `mobile-navigation.spec.ts`, `reduced-motion.spec.ts`, `responsive.spec.ts`. Fixture unitaire `usePageSeo.spec.ts` renommée `/interne-fixture` |
| DEV-033 | 6A | Scaffolder `apps/api` Symfony 7.4 LTS (PHP 8.4, sans DB, sans Doctrine) | Haute | Terminé | `apps/api/{composer.json,Dockerfile.dev,public/index.php,bin/console,src/Kernel.php,config/{bundles.php,services.yaml,services_test.yaml,packages/*.yaml,routes.yaml,preload.php},phpunit.xml.dist,tests/bootstrap.php,.env,.env.test,.gitignore,.dockerignore}`. Kernel `MicroKernelTrait`, bundles minimaux (`FrameworkBundle`, `MonologBundle`). Serializer, Validator, Mailer, RateLimiter, HttpClient, Uid activés | Migration vers FrankenPHP ou PHP-FPM planifiée dès qu'un domaine persistant ou une charge réelle apparaît (cf. ADR-006) |
| DEV-034 | 6A | Implémenter l'endpoint `POST /api/contact` (DTO + controller + service + sender + turnstile + rate limiter + origin allowlist) | Haute | Terminé | `src/Contact/Dto/ContactRequest.php` avec contraintes Validator (email, longueurs, choice, IsTrue consentement, regex téléphone). `src/Contact/Controller/{ContactSubmissionController,HealthController}.php`. `src/Contact/Service/{SubmitContactMessage,ContactMessageSenderInterface,SymfonyContactMessageSender,InMemoryContactMessageSender,ContactSubmissionResult}.php`. `src/Contact/Security/{OriginAllowlist,ContactRateLimiter,TurnstileVerifierInterface,TurnstileVerifierFactory,AlwaysAllowTurnstileVerifier,CloudflareTurnstileVerifier,TurnstileVerdict}.php`. `src/EventListener/ApiExceptionListener.php` convertit toute exception en JSON. Pipeline : Origin (403) → payload > 10 KB (413) → rate limit (429 + Retry-After) → JSON parsing (400) → honeypot silencieux (202) → validation (400) → Turnstile (403) → envoi + réponse 200. `Request-Id` UUID v7 dans réponse + header + logs. Aucun PII dans les logs | Widget Turnstile côté navigateur et formulaire réel en Phase 6B |
| DEV-035 | 6A | Écrire la suite PHPUnit (DTO, endpoint, mailer, logs sans PII, security) | Haute | Terminé | 8 fichiers de test : `ContactRequestValidationTest` (data provider 6 cas invalides), `OriginAllowlistTest` (4 cas), `TurnstileVerifierFactoryTest` (3 cas dont fail-closed sur secret manquant), `CloudflareTurnstileVerifierTest` (4 cas via `MockHttpClient`), `HealthControllerTest`, `ContactSubmissionControllerTest` (7 cas : accepté, Origin absent, Origin refusé, honeypot 202 générique, validation 400, JSON invalide, payload 413, headers réponse), `ContactRateLimitTest` (429 + Retry-After), `SymfonyContactMessageSenderTest` (From app / Reply-To visiteur / X-Request-Id / null recipient throws), `ContactLoggingTest` (aucune PII dans les logs sur happy path). `services_test.yaml` remplace le sender réel par `InMemoryContactMessageSender` | Étendre à la couverture email réel (Symfony Mailer transport) en Phase 6B ou 8 |
| DEV-036 | 6A | Wiring Caddy en frontal (Caddyfile + Dockerfile + compose.yaml) | Haute | Terminé | `infra/caddy/{Caddyfile,Dockerfile}`. Matcher `@api path /api/*` → `reverse_proxy api:8000` + `uri strip_prefix /api`. Fallback → `reverse_proxy web:3000`. `X-Forwarded-*` propagés. `compose.yaml` refactorisé : `caddy` seul service avec port hôte (`3001:80`), `web` et `api` en `expose:` interne seulement. `depends_on: [web, api]`. Volumes `api_vendor`, `api_var` ajoutés. Curl smoke : `curl http://localhost:3001/api/health` → `{"status":"ok"}` | En production, ajouter TLS Caddy automatique et configurer `NUXT_PUBLIC_SITE_URL` sur le domaine réel |
| DEV-037 | 6A | Ajouter le job GitHub Actions `api-quality.yml` pour Symfony | Haute | Terminé | `.github/workflows/api-quality.yml` : PHP 8.4 (shivammathur/setup-php), cache Composer, `composer validate --strict`, `composer install`, `lint:yaml config --parse-tags`, `lint:container --env=test`, `phpunit`. Déclenché sur `apps/api/**` + workflow lui-même, concurrency par ref. Complète le job `web-quality.yml` déjà en place | Ajouter un job d'analyse statique (PHPStan / Psalm) en Phase 8 quand le domaine métier grossit |
| DEV-038 | 6A | Stabiliser Playwright : supprimer les 5 `page.waitForLoadState('networkidle')` et rendre les attentes déterministes | Haute | Terminé | Remplacés dans `mobile-navigation.spec.ts`, `keyboard-navigation.spec.ts` (2 occurrences), `reduced-motion.spec.ts`, `home-cta-final.spec.ts` : `page.goto('/', { waitUntil: 'domcontentloaded' })` + `locator(<élément hydraté>).waitFor({ state: 'visible' })`. CI garde `workers: 1`, `retries: 1`, build Nitro préalable. Reporter CI enrichi : `[['line'], ['github'], ['html', ...]]` pour synthèse passed/flaky/failed en stdout | Ajouter un job matrix Firefox/WebKit uniquement quand la couverture visuelle multi-navigateur devient nécessaire |
| DEV-039 | 6A | Documenter Phase 6A (ADR-006 + ADR-007 + README + apps/api/README + 05/06/07/08) | Haute | Terminé | `docs/adr/ADR-006-runtime-symfony-caddy.md` (runtime PHP built-in server + Caddy, PostgreSQL différé), `docs/adr/ADR-007-endpoint-contact-securite.md` (CSRF Option B, Turnstile, rate limit, logging sans PII, table des réponses HTTP). `README.md` racine mis à jour (architecture 3 services, démarrage curl, variables backend). `apps/api/README.md` créé (stack, pipeline, dev local, contrôles qualité, table des variables). `docs/05-SECURITY-PRIVACY.md` §14.9 et §14.11 étendus. `docs/06-ARCHITECTURE-CODE.md` §16.1, §16.2, §16.10 mis à jour (Phase 6A vs différé, ADR renumérotés). `docs/07-QUALITY-DELIVERY.md` §20 : table des jobs CI actifs + note Playwright | Étendre la note d'incident dès qu'un premier abus réel est constaté sur `/api/contact` (Phase 6B ou 7) | Aucune |

### Statuts autorisés

- `À faire`
- `En cours`
- `Bloqué`
- `En revue`
- `Terminé`
- `Abandonné`

---

## 25. Journal des décisions

| Date | ID | Décision | Raisons | Conséquences |
|---|---|---|---|---|
| 2026-08-01 | DEC-001 | Présenter Devzair comme une agence digitale à taille humaine | Refléter l’offre globale et le fonctionnement projet | Employer « nous », ne pas utiliser le positionnement freelance |
| 2026-08-01 | DEC-002 | Utiliser Nuxt 4 avec Vue et TypeScript strict | SSR, routage, métadonnées et maintenabilité | Ne pas construire une SPA Vue pure |
| 2026-08-01 | DEC-003 | Utiliser un monorepo Docker Compose | Préparer l’ajout de Symfony sans restructuration | Git unique à la racine |
| 2026-08-01 | DEC-004 | Utiliser Symfony 7.4 LTS pour l’API future | Stabilité et maintenance longue | API ajoutée dans `apps/api` |
| 2026-08-01 | DEC-005 | Appliquer SOLID de façon pragmatique | Séparer les responsabilités sans surarchitecture | Pages, composants, composables, services et repositories distincts |
| 2026-08-01 | DEC-006 | Centraliser les métadonnées communes | Éviter les incohérences et répétitions | Création de `usePageSeo` |
| 2026-08-01 | DEC-007 | Pré-rendre les pages marketing et garder le SSR pour le blog initial | Performance et fraîcheur | Cache dynamique décidé plus tard |
| 2026-08-01 | DEC-008 | Ne pas installer de modules pendant le scaffolding | Base maîtrisée | Modules ajoutés avec contrôle et tests |
| 2026-08-01 | DEC-009 | Utiliser des imports explicites (Vue, composables, composants layout) plutôt que les auto-imports Nuxt dans les composants testés | Vitest n'exécute pas la transformation d'auto-import : dépendre de celle-ci masque les erreurs et rend la suite fragile | Chaque composant sous test importe explicitement ses dépendances, `~/composables` et `~/components/layout` |
| 2026-08-01 | DEC-010 | Refs de module (pas `useState`) pour `useMobileNavigation` | Le menu mobile est un singleton par onglet ; l'hydratation SSR n'a pas besoin d'être partagée par clé, et `useState` couple le composable au runtime Nuxt (donc à Vitest) | Composable pur, testable sans mocks Nuxt |
| 2026-08-01 | DEC-011 | Épingler `typescript` à `~5.9.0` | `vue-tsc` 3.3 ne supporte pas encore les changements d'exports internes de TypeScript 7 (`./lib/tsc`) | À revoir quand `vue-tsc` publie une version compatible TS 7 |
| 2026-08-01 | DEC-012 | Ne pas publier de coordonnées avant validation | Règle 1 (rien inventer) ; `site.contact.{email,phone,city}` = `null` | Le footer ne rend rien pour les coordonnées absentes |
| 2026-08-02 | DEC-013 | Installer les dépendances **au build Docker** (`npm ci` dans `Dockerfile.dev`) et exposer `node_modules` via un volume nommé | Éviter un `npm install` à chaque `docker compose up` (lent, sensible aux différences d’UID hôte, source de désync `package-lock`) | Rebuild explicite requis quand `package-lock.json` change |
| 2026-08-02 | DEC-014 | Rester en `USER root` dans le Dockerfile de dev | Compatibilité avec le bind mount `./apps/web:/app` (UID hôte différent selon la machine) | Le Dockerfile de production, à écrire en Phase 12, utilisera `USER node` |
| 2026-08-02 | DEC-015 | Utiliser Flat Config pour ESLint via `@nuxt/eslint` | Format supporté officiellement par Nuxt 4 et ESLint 10 ; permet d’ajouter nos règles sans casser la base auto-générée | Toute règle projet vit dans `apps/web/eslint.config.mjs` (surcouche de `./.nuxt/eslint.config.mjs`) |
| 2026-08-02 | DEC-016 | Choisir `@nuxt/fonts` pour l’auto-hébergement des polices | Détecte les `font-family` du CSS, télécharge au build, sert depuis notre origine, injecte un fallback métrique (size-adjust) pour limiter le CLS. Aucun CDN tiers en runtime | Les fichiers woff2 manuels de `/public/fonts/` deviennent inutiles ; on ne référence plus de fichier absent |
| 2026-08-02 | DEC-017 | Prop `external` sur `BaseButton` et `BaseLink` pour les liens vers pages non encore livrées | `NuxtLink` interne appelle `router.resolve` et émet `[VUE_ROUTER_R0004] No match found` pour toute route inexistante ; `external` force un `<a href>` neutre | Retirer la prop à mesure que les pages cibles sont créées (Phase 5) |
| 2026-08-02 | DEC-018 | CI GitHub Actions déclenchée uniquement sur changements de `apps/web/**` ou du workflow lui-même | Éviter les runs superflus pour de la doc pure ; garder le feedback rapide (~5-10 min) | Ajouter un workflow séparé pour Symfony en Phase 8 |
| 2026-08-02 | DEC-019 | Exécuter Playwright dans l'image officielle `mcr.microsoft.com/playwright:v1.62.1-noble`, hors du conteneur `web` (Alpine) | Les binaires Chromium fournis par Playwright ne tournent pas sur Alpine ; l'image officielle embarque Chromium + toutes les libs système, et est immuable par version. Solution A du brief Phase 3 | En local : soit sur la machine hôte après `npx playwright install`, soit via `docker run` sur l'image officielle en pointant `PLAYWRIGHT_BASE_URL` vers `localhost:3001`. En CI : job `e2e` dédié, parallèle au job `quality` |
| 2026-08-02 | DEC-020 | Forcer `await page.emulateMedia({ reducedMotion: 'reduce' })` dans le `beforeEach` de `reduced-motion.spec.ts` au lieu de compter sur `test.use({ reducedMotion: 'reduce' })` | Dans notre configuration Playwright 1.62 + Chromium, `test.use` ne propage pas toujours l'option au bon moment de l'hydratation Vue ; `matchMedia('(prefers-reduced-motion: reduce)').matches` restait `false` au premier assert | Systématiser `page.emulateMedia` pour tout test dépendant d'une media-query |
| 2026-08-02 | DEC-021 | Retenir uniquement `@nuxtjs/robots` et `@nuxtjs/sitemap` du bundle Nuxt SEO (Option 2 « à la carte »), gérer Schema.org à la main | Deux entités Schema.org stables (Organization + WebSite) ne justifient pas `nuxt-schema-org` ; `nuxt-link-checker` et `nuxt-seo-utils` n'ont aucun bénéfice sur 2 routes réelles. Cf. ADR-004 | Réévaluer à l'ouverture de la Phase 9 (blog, Article, BreadcrumbList) |
| 2026-08-02 | DEC-022 | `NUXT_PUBLIC_SITE_INDEXABLE` = `false` par défaut, seule source de vérité pour toute la politique d'indexation | La preprod ne doit jamais devenir indexable par simple oubli d'une variable ; mirroir unique vers `runtimeConfig.public`, `site.indexable`, `@nuxtjs/robots` | La production doit explicitement forcer `true` — à documenter dans la recette Phase 12 |
| 2026-08-02 | DEC-023 | Une page `noindex` n'émet **pas** de balise canonical | Un canonical sur une page noindex renvoie un signal contradictoire à un moteur qui l'aurait crawlée ; mieux vaut n'émettre aucun signal d'URL préférée | Test unitaire dédié (`usePageSeo — page noindex`) et test E2E `/design-preview reste noindex sans canonical` |
| 2026-08-02 | DEC-024 | Le canonical est calculé exclusivement à partir de `NUXT_PUBLIC_SITE_URL` + `path`, jamais depuis le header HTTP Host | Le header Host peut être manipulé (proxy mal configuré, attaque) et diffère selon l'environnement (Docker interne 3000 vs public 3001) ; la source doit être immuable et contrôlée par le déploiement | `buildCanonical()` accepte uniquement `siteUrl` + `path` ; commentaire explicite dans `app/utils/site-url.ts` |
| 2026-08-02 | DEC-025 | Différer la création de l'image Open Graph par défaut plutôt que publier un placeholder | Règle 1 (ne rien inventer) : une image OG représente l'agence ; sans validation client elle ne doit pas apparaître dans le HTML | `apps/web/public/og/.gitkeep` documente l'emplacement ; `defaultOgImage = null` ; à livrer en Phase 5 |
| 2026-08-02 | DEC-026 | Différer le pré-rendu de `/` en Phase 5 | La page d'accueil actuelle est un placeholder noindex ; la pré-rendre figerait un contenu volatile | `routeRules` vide, commentaire dans `nuxt.config.ts` ; à activer quand la vraie home Phase 5 sera livrée |
| 2026-08-02 | DEC-027 | Rendre le graphe SVG accessible par `role="img"` + `<title>`/`<desc>` liés via `aria-labelledby`, plutôt que par une liste ARIA de nœuds | Le graphe est informatif, pas interactif : une seule annonce (« Les cinq pôles… ») + une description longue suffisent. Une liste ARIA de nœuds serait doublement lue avec la version texte de la page (règle éditoriale : les 5 pôles réapparaissent en Phase 5B) | Les sous-groupes graphiques sont `aria-hidden="true"`, aucun `tabindex` n'est posé, les ids `aria-labelledby` sont générés par `useId()` (SSR-safe, aucune collision si le composant est rendu plusieurs fois) |
| 2026-08-02 | DEC-028 | Animation signature en CSS pure (stroke-dasharray + opacity + transform), non bouclée, ~1,4 s | Pas de dépendance d'animation, pas de JS, `prefers-reduced-motion` déjà géré par `animations.css` (règle globale `!important` sur `animation-duration`) — mais celle-ci ne rétablit pas les états initiaux (`stroke-dashoffset:200`, `opacity:0`). Une règle locale de défense en profondeur force l'état final | Ajouter des animations « décoratives » suit le même pattern : durée finie, `animation-fill-mode: forwards`, media-query locale forçant l'état final |
| 2026-08-02 | DEC-029 | Séparer les positions graphiques (x/y/labelAnchor/labelDx/labelDy) et les données métier (label, description, order, variant) des cinq pôles | Une position (240, 75) n'a de sens que dans un SVG 480×480 ; l'exposer via `expertisePillars` polluerait un contrat qui doit rester consommable par la future page `/expertises`, la grille Phase 5B et un éventuel export IA | `expertisePillars` reste une source unique de vérité pour label/description/ordre ; le tableau `positions` local à `HomeEcosystemGraph.vue` est le point d'extension pour toute nouvelle mise en page graphique |
| 2026-08-02 | DEC-030 | Les CTA de l'accueil pointent vers les ancres `#contact` et `#realisations` tant que les pages/sections cibles n'existent pas | Interdit d'introduire des routes vides ou de sortir l'accueil vers un placeholder ; l'ancre reste utilisable (au pire, no-op) et remplaçable en une ligne quand la vraie cible sera livrée | Retirer `external` et remplacer `to="#contact"` par `to="/contact"` dès que la page `/contact` est publiée (Phase 6) ; idem pour `/realisations` (Phase 7) |
| 2026-08-02 | DEC-031 | Étendre `expertise-pillars.ts` avec `longDescription` et `services` plutôt que renommer `description` (qui reste consommée par HomeEcosystemGraph pour le libellé court dans le SVG) | Le SVG du hero affiche « design · UX · identité » comme sous-titre court, la grille détaillée a besoin d'une phrase narrative et de 3 livrables. Renommer aurait cassé le SVG sans bénéfice. Le contrat garde une seule source de vérité pour chaque niveau de granularité | Toute nouvelle vue des pôles (page `/expertises`, exports IA…) doit préférer `longDescription` pour un discours narratif, `description` pour un libellé court en contexte contraint |
| 2026-08-02 | DEC-032 | Ajouter une variante `accent` (Faire évoluer) à côté des variantes existantes `primary` (Construire) et `default` | `HomeExpertisePillars` a besoin d'un fond petrol distinct sur le dernier pôle pour marquer la clôture du parcours. Le SVG hero, lui, ne check que `variant === 'primary'` : tout ce qui n'est pas primary rend en navy-elevated → l'ajout est rétro-compatible sans toucher au SVG | Toute nouvelle vue peut consommer les 3 variantes ; ne pas confondre `primary` (accent hero) et `accent` (accent grille) — ce sont deux emphases indépendantes |
| 2026-08-02 | DEC-033 | Carrousel mobile CSS-natif (`scroll-snap-type: x mandatory`) plutôt qu'une bibliothèque JS | Aucune dépendance ajoutée, fonctionne sans JS (SSR + progressive enhancement), pas de piège d'accessibilité (les 5 cartes sont toujours dans le DOM, indexables, lisibles au lecteur d'écran, sans bouton fantôme prev/next). Coût nul en bundle | Réévaluer uniquement si un besoin métier réel apparaît (autoplay, tabs synchronisés, animation contrôlée) — sinon rester natif |
| 2026-08-02 | DEC-034 | Sur fond petrol ou navy-elevated, ne pas utiliser Devzair-blue (`#2e86d9`) pour du petit texte gras | Contraste WCAG 2.2 AA insuffisant : 2.08:1 sur petrol, 4.41:1 sur navy-elevated (seuil 4.5:1 pour petit texte gras). Documenté par Axe rule `color-contrast`. Cf. DEV-028 | Utiliser cream / cream-muted pour les tags/index sur ces surfaces. Devzair-blue reste utilisable comme accent décoratif sur navy de base (7:1+) et sur cream/sand (5:1+) |
| 2026-08-02 | DEC-035 | Publier la section « Réalisations » en variante « état honnête » plutôt qu'avec un cas fictif, un logo générique ou un skeleton loader | Règle 1 (AGENTS.md, DEC-001) interdit d'inventer un client, un chiffre ou une preuve. DEV-003 n'est pas encore livré. L'ancre `#realisations` doit néanmoins exister (le CTA hero et la nav header y pointent), donc la section est présente avec un titre externe permanent (« Des solutions concrètes… ») et un placeholder honnête interne | Le jour où un cas réel est publié, remplacer le bloc `home-case__placeholder` par la variante détaillée sans casser l'ancre. Ne jamais introduire de « logo cloud » ou de témoignage anonyme pour combler l'intervalle |
| 2026-08-02 | DEC-036 | Le lien header « Réalisations » cible `/#realisations` (ancre same-page) plutôt qu'une route `/realisations` non livrée | La page dédiée n'existe pas encore (Phase 7). Pointer vers une route absente casse la nav et rompt la promesse « pas de route vide » (DEC-017). L'ancre est active, la section existe (même en variante honnête), l'utilisateur atterrit sur un contenu | Migrer vers `/realisations` uniquement quand la page dédiée sera livrée (Phase 7). Même logique appliquée si d'autres sections one-pager deviennent des routes plus tard |
| 2026-08-02 | DEC-037 | Six colonnes pour la méthode desktop refusées au profit d'une grille 3×2 dès 1024px | Six colonnes contenant un H3 + un paragraphe de ~35 mots à 1024px produisent un texte serré et une hiérarchie visuelle floue. Le brief 5C §9 autorise explicitement le fallback 3×2. La lisibilité prime sur la fidélité littérale à un layout 6 colonnes | Réévaluer uniquement si le contenu devient significativement plus court (titre + 1 phrase max) ; garder 3×2 sinon |
| 2026-08-02 | DEC-038 | Publier `HomeCallToAction` sans bouton tant que `site.contact.email` est `null`, plutôt qu'un mailto vide, un `href="#"` ou un bouton désactivé | Règle AGENTS.md #1 « ne jamais inventer un moyen de contact » ; un bouton non fonctionnel dégrade la confiance et peut ouvrir le client mail sur une adresse vide (mailto:) ou déclencher un scroll parasite (`#`). La section garde son ancrage `#contact` opérationnel — seul le CTA est conditionnel | Ajouter un mailto BaseButton automatiquement dès que `site.contact.email` sera défini. En Phase 6, remplacer le bloc par le vrai formulaire (validation serveur, anti-spam) |
| 2026-08-02 | DEC-039 | Publier la notice « Le moyen de contact en ligne sera activé avant la mise en production. » uniquement si `!contact.email && siteIndexable === false` | Cette notice rassure un lecteur interne (preprod) mais divulgue un détail d'implémentation qui n'a rien à faire en production. Le double garde-fou (contact absent **et** env non indexable) empêche toute fuite si la production reste transitoirement sans coordonnée | Retirer automatiquement dès que `site.contact.email` est défini, ou en production (`NUXT_PUBLIC_SITE_INDEXABLE=true`). Aucune modification du composant nécessaire |
| 2026-08-02 | DEC-040 | Lire l'indexabilité dans un composant via `useRuntimeConfig().public.siteIndexable`, jamais `process.env` | `process.env` n'est pas défini côté client Nuxt et bypasserait la source unique posée par DEC-022. `runtimeConfig.public` est mirrorée à partir des variables `NUXT_PUBLIC_*` par Nuxt lui-même, testable via un stub (`test/setup.ts` installe un défaut, chaque test peut le surcharger) | Toute lecture d'une variable publique dans un composant doit passer par `useRuntimeConfig().public.*` — vérifier au review |
| 2026-08-02 | DEC-041 | Supprimer `/design-preview` à la clôture Phase 5D plutôt que la conserver en interne noindex | La page a rempli son rôle d'atelier design system pendant Phases 3 et 4 : Axe, contrôles clavier, contrastes. Depuis Phase 5, l'accueil réelle expose les mêmes composants dans leur contexte de production, avec plus de surface (8 sections vs 2 blocs de démo). Conserver `/design-preview` doublerait la maintenance sans bénéfice — et la sortie d'un noindex par erreur exposerait un contenu de démo. La coverage utile a été migrée dans `test/e2e/home-structure.spec.ts` | Aucun besoin de page interne pour les futurs lots ; les composants isolés sont couverts par Vitest (props, variantes), l'intégration par les E2E sur `/` |
| 2026-08-02 | DEC-042 | Retenir PHP 8.4 CLI `php -S` pour la Phase 6A plutôt que FrankenPHP ou PHP-FPM/Nginx | Phase 6A : zéro DB, un endpoint POST + un endpoint GET, aucune session, aucun asset servi par PHP. `php -S` derrière Caddy suffit, se documente en une ligne dans le Dockerfile, ne réclame ni pool ni supervisord, et permet un `bin/console` reproductible en dev. Documenté dans ADR-006 | Migrer vers FrankenPHP (worker) ou PHP-FPM dès qu'un domaine persistant (PostgreSQL/Doctrine), une charge réelle (>QPS quotidien mesurable) ou un besoin de serveur d'assets apparaît. Déclencheurs listés en ADR-006 |
| 2026-08-02 | DEC-043 | Adopter la CSRF « Option B » stateless (allowlist stricte de l'`Origin` + Turnstile + rate limit) au lieu d'un jeton CSRF par session | Le site Nuxt est majoritairement pré-rendu ; introduire une session pour émettre un jeton CSRF casserait le prerender et ajouterait un cookie inutile. La combinaison Origin (rejette les POST cross-site classiques), Turnstile (rejette les bots headless) et rate limiter (limite les abus) offre une défense en profondeur adaptée à un formulaire de contact. Documenté dans ADR-007 | Toute requête sans `Origin` correspondant à `CONTACT_ORIGIN_ALLOWLIST` est refusée en 403 `origin_not_allowed`. Ajouter une origine → l'ajouter à la variable d'environnement (jamais dans le code). Si un espace client authentifié est introduit plus tard, réévaluer et repasser à un jeton par session pour ce périmètre |
| 2026-08-02 | DEC-044 | `MAILER_DSN=null://null` par défaut, aucun email envoyé tant que la boîte réelle n'est pas branchée | Un défaut d'envoi bloqué par configuration est infiniment moins grave qu'un email involontaire (dev qui atteint la boîte prod, spam de test à la clôture d'un CI). Le NullTransport de Symfony consomme l'`Email` sans erreur, donc l'endpoint continue de répondre 200 et les tests d'intégration passent | Documenter la variable dans `.env.example` et dans `README.md`. En production, changer explicitement `MAILER_DSN` et surveiller les premiers envois via le log `contact` (canal dédié) — `Request-Id` permet le rapprochement |
| 2026-08-02 | DEC-045 | Aucun PII dans les logs de l'endpoint contact (ni message, ni email, ni téléphone, ni token Turnstile, ni secret) | Un log est copié sur disque, roulé, potentiellement exporté vers un agrégateur externe : c'est un canal à faible contrôle. Le RGPD exige la minimisation (docs/05 §15) et un incident sur les logs est un incident sur des données personnelles. Le `Request-Id` (UUID v7 dans le log + réponse + header `X-Request-Id`) permet le support sans exposer le contenu | Le test `ContactLoggingTest` échoue si une valeur PII marquée (`SECRET_*`) apparaît dans les records Monolog sur le happy path. Ne pas ajouter de nouveau `logger->info()` dans le domaine contact sans passer par les mêmes canaux + tester la non-fuite |
| 2026-08-02 | DEC-046 | `OriginAllowlist` normalise le trailing slash (`https://foo.com/` ≡ `https://foo.com`) et refuse toute requête sans `Origin` | Le navigateur envoie parfois `https://foo.com` et parfois `https://foo.com/` selon le contexte (POST XHR vs form) ; imposer un match strict conduirait à des faux refus dépendant du canal d'appel. L'absence d'`Origin` (curl anonyme, script serveur) est refusée car incompatible avec le cas d'usage réel (formulaire navigateur) | Ajouter une origine dans `CONTACT_ORIGIN_ALLOWLIST` (variable, séparateur virgule) sans se soucier du slash final. Refuser explicitement `null` origin (attaque connue) |
| 2026-08-02 | DEC-047 | `TurnstileVerifierFactory` échoue au boot si `TURNSTILE_ENABLED=true` sans `TURNSTILE_SECRET` (fail-closed), plutôt qu'un fallback silencieux vers l'implémentation permissive | Un site qui croit avoir un anti-bot actif mais qui accepte tout est plus dangereux qu'un site clairement sans anti-bot : les alertes sont fausses, les logs mensongers, et la porte est ouverte. Un `RuntimeException` au boot est bruyant, immédiat et vu par le déploiement | En dev/test : `TURNSTILE_ENABLED=false` par défaut → `AlwaysAllowTurnstileVerifier` explicite. En production : le secret est obligatoire, injecté par le gestionnaire de secrets, jamais dans Git |
| 2026-08-02 | DEC-048 | Générer un `Request-Id` UUID v7 pour chaque requête et l'exposer simultanément dans le corps JSON, l'en-tête `X-Request-Id` et les logs | UUID v7 est ordonné dans le temps → tri chronologique naturel des incidents sans champ `date` séparé. La triple exposition (body/header/log) permet à un visiteur qui reporte un incident de fournir un identifiant reproductible et au support de le retrouver instantanément dans les logs, sans exposer de PII | Toute réponse d'erreur de l'endpoint inclut `request_id` — utiliser cet identifiant comme clé de corrélation pour tout ticket support. Ne jamais réutiliser un `Request-Id` (généré à chaque requête, pas depuis le client) |
| 2026-08-02 | DEC-049 | `From` = adresse applicative Devzair (jamais l'email du visiteur), `Reply-To` = email du visiteur | Mettre l'email visiteur en `From` casse SPF/DKIM/DMARC et envoie l'email en spam. `Reply-To` permet une réponse naturelle en un clic depuis le client mail Devzair, sans compromettre l'authentification. Aucun HTML dans le corps (texte brut) élimine toute surface d'injection dans le rendu | La variable `CONTACT_FROM_EMAIL` doit rester sous domaine Devzair aligné SPF/DKIM. Ne jamais construire un en-tête email avec une entrée utilisateur brute (règle §14.9 du référentiel) |
| 2026-08-02 | DEC-050 | Caddy est le seul service à publier un port sur l'hôte ; `web` et `api` restent en `expose:` interne dans compose.yaml | Réduire la surface d'attaque en développement (aucun daemon Nuxt/Nitro ou PHP directement accessible depuis la LAN de dev), reproduire fidèlement l'architecture de production (un seul frontal), et permettre à Caddy d'ajouter TLS/HSTS/rate-limit sans dupliquer la config par service | `NUXT_PUBLIC_SITE_URL=http://localhost:3001` (Caddy). Aucun test ne doit taper directement `http://localhost:3000` ou `http://localhost:8000`. En prod : ajouter `tls internal` ou domaine automatique + `strict_transport_security`, et retirer `admin off` uniquement si l'admin socket est sécurisée par ailleurs |
| 2026-08-02 | DEC-051 | Pour toute interaction Playwright qui déclenche un handler Vue attaché au client (ex : ouverture du menu mobile), utiliser un pattern de clic retentable fondé sur l'état contractuel du DOM (`aria-expanded`), pas sur une sonde d'`__vue_app__` ni sur un délai fixe | En mode `nuxt dev` (Vite), `document.getElementById('__nuxt').__vue_app__` est posé par `mount()` avant que Vue n'ait effectivement attaché ses listeners `@click` sur le DOM initial. Un premier clic Playwright peut être dispatché *sans* handler : le DOM click event est bien émis, aucun listener ne le reçoit, l'état ne bascule pas, et le test échoue silencieusement sur l'assertion suivante (`expect(dialog).toBeVisible()` timeout). L'introspection de `__vue_app__` est nécessaire mais non suffisante. Un `waitForTimeout` masquerait la race sans la caractériser | Helper unique `openMobileNavigation(page)` dans `test/e2e/support/mobile-nav.ts` : `expect(async () => { await button.click(); await expect(button).toHaveAttribute('aria-expanded', 'true', { timeout: 500 }) }).toPass({ timeout: 10_000, intervals: [100, 200, 500] })`. Chaque nouvelle interaction sensible à l'hydratation (ex : soumission de formulaire côté client) doit adopter le même pattern : action → assertion sur un état observable contractuel → `toPass` avec intervals. Ne pas augmenter les retries Playwright ni les timeouts globaux pour masquer une race |

---

## 26. Journal des changements

| Date | Version | Changement |
|---|---|---|
| 2026-08-01 | 1.0 | Création du référentiel initial |
| 2026-08-01 | 2.0 | Architecture Nuxt/Docker/Symfony confirmée, phases réorganisées, SOLID/DRY et implémentation SEO/GEO Nuxt ajoutés |
| 2026-08-01 | 2.1 | Lot design system posé (Phase 3 partielle) : tokens CSS, composants base, layout (header/mobile nav/footer), composable `useMobileNavigation`, page interne `/design-preview` noindex, Vitest opérationnel (20 tests verts), typecheck et build verts |
| 2026-08-02 | 2.2 | Clôture des fondations techniques (Phases 1 et 2) : `.gitignore` racine, `Dockerfile.dev` reproductible (npm ci au build, plus de `npm install` à chaque boot), `compose.yaml` refactorisé, `.env.example` sans secret, ESLint Flat Config via `@nuxt/eslint`, Playwright installé avec smoke test `/design-preview`, `@nuxt/fonts` en charge des polices, workflow CI `web-quality.yml`, READMEs racine et `apps/web` mis à jour. `npm run quality` vert (lint, typecheck, tests 20/20, build) |
| 2026-08-02 | 2.3 | Clôture Phase 3 — Design system et accessibilité : suite E2E Playwright complète (6 fichiers, 28 tests) sur `/design-preview` (H1/robots/skip-link/header/footer/console, mobile-navigation avec focus-trap et scroll-lock, keyboard-navigation avec anneau focus, responsive 390/768/1440, reduced-motion, Axe WCAG 2.2 AA sans `disableRules`). Défauts démontrés corrigés (SiteFooter contraste `--color-cream-subtle`, swatch Devzair blue en noir pur). Job CI `e2e` dans l'image officielle Playwright, scripts `test:e2e:ui` et `quality:full`. Conventions de nommage documentées dans `docs/06-ARCHITECTURE-CODE.md` §16.9. `npm run quality:full` vert (lint, typecheck, tests unitaires 20/20, build, E2E 28/28) |
| 2026-08-02 | 2.5 | Phase 5A — Accueil : hero + graphe SVG des cinq pôles. `HomeHero.vue` (eyebrow, H1, introduction, deux CTA vers `#contact` et `#realisations`, réassurance à 2 items), `HomeEcosystemGraph.vue` (SVG accessible `role="img"` + `<title>`/`<desc>` liés par `aria-labelledby` via `useId()`, sous-groupes `aria-hidden`, animation signature CSS ~1,4 s non bouclée, défense en profondeur `prefers-reduced-motion`), `app/config/expertise-pillars.ts` (contrat typé, source unique pour les cinq pôles). `pages/index.vue` recâblée sur `usePageSeo({ title: "Agence digitale pour sites web…", type: "website" })`, canonical `/`. `routeRules['/'] = { prerender: true }` : deux routes prérendues au build (`/` + `/_payload.json`). Image OG statique reportée (aucun logo Devzair disponible — cf. DEV-026). Curl SSR : `<html lang="fr">`, `og:type=website`, `twitter:card=summary` (pas d'image), aucun `og:image` émis. Tests : 69 unitaires (+17) et 51 E2E (+13) verts, `npm run quality` OK. |
| 2026-08-02 | 2.7 | Phase 5C — Accueil : sections « Réalisations » (état honnête), « Notre méthode », « Pourquoi Devzair ». Deux configs typées créées (`app/config/project-process.ts` 6 étapes, `app/config/trust-promises.ts` 5 promesses, texte verbatim brief 5C). Trois composants : `HomeFeaturedCaseStudy.vue` (ancre `#realisations`, variante « en préparation » — aucun faux client, aucun logo, aucun bouton vers route absente ; cf. DEC-035), `HomeProcess.vue` (`<ol>` 6 étapes verbatim `Découverte`/`Cadrage`/`Conception`/`Développement`/`Lancement`/`Évolution`, timeline verticale mobile via pseudo-éléments, 2 col ≥640px, grille 3×2 ≥1024px — 6 col refusées pour la lisibilité, cf. DEC-037), `HomeTrust.vue` (`<ul>` 5 promesses verbatim, 1 col mobile, 2 col+row-span ≥640px, grille asymétrique 3+2 centrée ≥1024px). `pages/index.vue` orchestre 7 sections. `navigation.ts` : lien header « Réalisations » recablé de `/realisations` vers `/#realisations` (cf. DEC-036). CTA hero « Découvrir nos réalisations » opérationnel sans modification. Sémantique WCAG 2.2 AA respectée : un seul H1, un H2 par section, aucune interaction fantôme. Curl SSR : 18 signatures éditoriales exactes présentes une seule fois (H2, honest-state, 6 labels méthode, 5 labels promesses, `id="realisations"`, `/#realisations`). Tests : 120 unitaires (+30) et 97 E2E (+25 dans `home-sections-secondary.spec.ts` — ordre 7 sections, honest-state, CTA hero, header nav, Axe, reduced-motion, responsive 320/390/768/1024/1440) verts. Build : 2.83 MB / 733 kB gzip (+45 KB / +8 KB gzip vs 5B). Sections différées : FAQ éditoriale, CTA final, ancre `#contact` (Phase 5D/6). |
| 2026-08-02 | 2.6 | Phase 5B — Accueil : sections « Le constat », « La réponse Devzair », « Nos expertises · 5 pôles ». `expertise-pillars.ts` étendu (`longDescription`, `services: readonly string[]`, variante `accent`) — 100 % rétro-compatible avec `HomeEcosystemGraph.vue` qui ne teste que `variant === 'primary'`. Trois composants créés : `HomeProblems.vue` (constat, `<ol>` 5 items, numérotation Space Mono, 2 colonnes ≥768px), `HomeConnectedApproach.vue` (parcours 5 étapes sur fond navy, source unique `expertisePillars`, flèches ↓/→ décoratives `aria-hidden`, `longDescription` en `sr-only` pour AT), `HomeExpertisePillars.vue` (ancre `#expertises`, carrousel scroll-snap mobile CSS-natif sans JS ni boutons, 2 col tablet, grille asymétrique 3×2 desktop avec Concevoir spans 2 rows et Faire évoluer petrol). `pages/index.vue` orchestre les 4 sections. Corrections Axe : `HomeConnectedApproach.step-index` passé sur cream (Devzair-blue échouait à 4.41:1 sur navy-elevated), carte accent passée sur cream/cream-muted (Devzair-blue échouait à 2.08:1 sur petrol) — cf. DEV-028 / DEC-034. Curl SSR : 3 H2 présents, 5 items par section, `id="expertises"` et « Faites défiler » présents. Tests : 90 unitaires (+21) et 72 E2E (+21) verts. Build : 2.79 MB / 725 kB gzip (+40 KB / +11 KB gzip). Sections différées documentées : réalisations Phase 5C/7, méthode / pourquoi / CTA final Phase 5C, contact Phase 6. |
| 2026-08-02 | 2.4 | Clôture Phase 4 — Socle SEO technique. Modules `@nuxtjs/robots` v5.7 et `@nuxtjs/sitemap` v7.6 (Option 2 « à la carte », cf. ADR-004). Utilitaires purs `site-url.ts` et `canonical.ts`. Composables `usePageSeo` (canonical + OG + Twitter + robots) et `useSiteSchema` (JSON-LD Organization + WebSite, injecté une fois par le layout). `runtimeConfig.public` étendu à `siteUrl` et `siteIndexable` (défaut `false` — safe par défaut, une seule source de vérité). Pages `/` et `/design-preview` migrées vers `usePageSeo({ noindex: true })`. Tests unitaires étendus (52/52) : `site-url.spec.ts`, `canonical.spec.ts`, `usePageSeo.spec.ts`. Tests E2E étendus (38/38) : `seo.spec.ts` couvre HTML SSR, X-Robots-Tag, robots.txt et sitemap.xml. Curl confirme : `<html lang="fr">`, titre `Devzair — Chantier en cours \| Devzair`, `meta robots noindex, nofollow`, JSON-LD Organization + WebSite, `og:*` absolus, aucune canonical sur pages noindex, `X-Robots-Tag: noindex, nofollow`, `robots.txt : User-agent: * / Disallow: /`, sitemap XML absolu sans `/design-preview`. Aucune donnée fictive (contact, sameAs, aggregateRating). Image OG par défaut différée (`public/og/.gitkeep`, `defaultOgImage = null`). `npm run quality` vert (lint, typecheck, tests 52/52, build). |
| 2026-08-02 | 2.9 | Phase 6A — Endpoint `POST /api/contact` (backend only, sans UI). Scaffold `apps/api` Symfony 7.4 LTS / PHP 8.4 sans DB, sans Doctrine, sans admin (cf. ADR-006). Caddy 2 devient l'unique service exposé sur l'hôte (`3001:80`), route `/api/**` vers `api:8000` avec `uri strip_prefix /api`, tout le reste vers `web:3000` (cf. DEC-050). Pipeline endpoint (cf. ADR-007) : Origin allowlist (`OriginAllowlist` normalise le trailing slash — DEC-046, 403 `origin_not_allowed`) → payload > 10 KB (413) → rate limiter token bucket par IP réelle (`limiter.contact_ip`, 429 + `Retry-After`) → JSON parse (400 `invalid_json`) → honeypot `website` silencieux (202 générique sans email) → validation stricte `ContactRequest` DTO (Symfony Validator : email, longueurs, regex téléphone, Choice `projectType`, IsTrue `consent`) → Turnstile serveur (`CloudflareTurnstileVerifier` + `TurnstileVerifierFactory` fail-closed au boot si secret manquant — DEC-047) → `SubmitContactMessage` → `SymfonyContactMessageSender` (`From` app / `Reply-To` visiteur / texte brut / `X-Request-Id` — DEC-049). `MAILER_DSN=null://null` par défaut (DEC-044). `Request-Id` UUID v7 dans le corps JSON + `X-Request-Id` + logs (DEC-048). Canal Monolog `contact`, aucun PII loggué (DEC-045), test `ContactLoggingTest` garde-fou. CSRF : Option B stateless, aucune session, aucun cookie (DEC-043). CI : nouveau workflow `.github/workflows/api-quality.yml` (PHP 8.4, `composer validate --strict`, `lint:yaml config`, `lint:container --env=test`, PHPUnit) déclenché sur `apps/api/**`. Suite PHPUnit 11.4 : `ContactRequestValidationTest` (data provider 6 cas invalides), `OriginAllowlistTest`, `TurnstileVerifierFactoryTest` (fail-closed), `CloudflareTurnstileVerifierTest` (MockHttpClient), `HealthControllerTest`, `ContactSubmissionControllerTest` (7 cas dont honeypot 202, validation 400, payload 413, headers), `ContactRateLimitTest` (429 + Retry-After), `SymfonyContactMessageSenderTest` (From app / Reply-To visiteur / X-Request-Id / null recipient throws), `ContactLoggingTest` (aucune PII sur happy path). Playwright stabilisé (DEV-038) : 5 `page.waitForLoadState('networkidle')` supprimés dans `mobile-navigation.spec.ts`, `keyboard-navigation.spec.ts` (2×), `reduced-motion.spec.ts`, `home-cta-final.spec.ts` — remplacés par `goto('/', { waitUntil: 'domcontentloaded' })` + `locator(<élément hydraté>).waitFor({ state: 'visible' })`. CI Playwright conserve `workers: 1`, `retries: 1`, reporter enrichi `[['line'], ['github'], ['html', ...]]` pour distinguer passed/flaky/failed en stdout. Nouveaux ADRs : ADR-006 (runtime PHP built-in server + Caddy, ADR-008 différé PostgreSQL/Doctrine), ADR-007 (CSRF Option B, 6 filtres, table des réponses HTTP, table logging). Docs mises à jour : `README.md` racine (architecture 3 services, smoke curl), `apps/api/README.md` créé (stack, pipeline, dev local, variables, non-goals explicites), `docs/05-SECURITY-PRIVACY.md` §14.9 + §14.11, `docs/06-ARCHITECTURE-CODE.md` §16.1/§16.2/§16.10 (ADR renumérotés), `docs/07-QUALITY-DELIVERY.md` §20 (jobs CI actifs + note Playwright). Non-goals confirmés Phase 6A : aucun formulaire Nuxt, aucun widget Turnstile navigateur, aucune page `/contact`, aucune PostgreSQL, aucun admin, aucun stockage de message, aucun CRM, aucune queue asynchrone, aucune image OG — livrés en Phase 6B ou plus tard. |
| 2026-08-02 | 2.10 | Correctif Playwright — élimination du test flaky `home-cta-final.spec.ts:128` (« mobile menu opens, activates the CTA, closes, and lands on #contact ») et stabilisation transverse des interactions clavier/menu mobile. **Cause racine** : en `nuxt dev`, `document.getElementById('__nuxt').__vue_app__` est posé par `mount()` avant que Vue n'ait attaché ses listeners `@click` sur le DOM initial ; le premier clic Playwright peut être dispatché sans handler → l'état `isOpen` ne bascule pas, `#mobile-navigation` n'est jamais rendu, et le test échoue en aval sur `expect(dialog).toBeVisible()`. L'ancien helper `waitForHydration` (introspection de `__vue_app__`) est nécessaire mais insuffisant. **Correction** (cf. DEC-051) : nouveau helper `test/e2e/support/mobile-nav.ts::openMobileNavigation(page)` qui retente `button.click()` tant que `aria-expanded` n'a pas basculé à `'true'`, via `expect(...).toPass({ intervals: [100, 200, 500] })`. Ancien helper `test/e2e/support/hydration.ts` supprimé (code mort). Tests migrés : `home-cta-final.spec.ts` (test CTA + capture erreurs console navigation/hydratation), `mobile-navigation.spec.ts` (3 tests : opens+focus, closes on link, tab-trap), `reduced-motion.spec.ts` (mobile nav ouverture). Assertions renforcées sur le test CTA : ajout `aria-expanded` true/false explicites et check absence d'erreurs console `/navigat|hydrat|router|teleport|vue|nuxt/i`. **Vérification** : test ciblé répété 20× sans retry → 20 passed. Suite complète 109 tests `--retries=0` → 109 passed. Suite complète en config CI habituelle (`workers=1 retries=1`) → 109 passed, 0 flaky, 0 failed. `npm run lint` + `npm run typecheck` + `npm run test` (130/130) + `npm run build` (2.8 MB / 728 kB gzip) + `php bin/phpunit` (31/31, 68 assertions) tous verts. Aucun `waitForTimeout` ajouté, aucun `force: true`, aucun timeout global augmenté, aucun retry augmenté. |
| 2026-08-02 | 2.8 | Clôture Phase 5D et Phase 5 — CTA final `#contact` + retrait de `/design-preview`. `HomeCallToAction.vue` (huitième section, ancre `#contact`, eyebrow `Parlons de votre projet`, H2 `Construisons une présence digitale à la hauteur de votre entreprise.`, paragraphe explicatif). Stratégie de contact conditionnelle (DEC-038) : mailto `BaseButton` si `site.contact.email` défini, sinon aucun bouton ni lien. Notice preprod (DEC-039) publiée uniquement si `!contact.email && useRuntimeConfig().public.siteIndexable === false` (DEC-040 : jamais `process.env`). `pages/index.vue` orchestre 8 sections dans l'ordre édito `docs/01-CONTENT.md §7.1`. `navigation.ts` : `primaryCta.to = "/#contact"` (source unique header desktop/footer/menu mobile) ; header nav « Expertises » recablé vers `/#expertises`. Suppression de `apps/web/app/pages/design-preview.vue` et `test/e2e/design-preview.spec.ts` (DEC-041) ; coverage utile migrée dans `test/e2e/home-structure.spec.ts` ; `test/e2e/seo.spec.ts` vérifie que `/design-preview` renvoie 404 ; sitemap : plus d'`exclude` (une seule route `/`). Tests : 130 unitaires (+10 `HomeCallToAction.spec.ts` avec `vi.mock('~/config/site')` pilotant email null/set × indexable true/false) et suites E2E `home-cta-final.spec.ts` (13) + `home-structure.spec.ts` (5) ; `home-sections-secondary.spec.ts` étendu à 8 sections ; `beforeEach` de 5 suites clavier/mobile/reduced-motion/responsive/accessibility migrés vers `/`. `npm run quality` vert (lint, typecheck, tests 130/130, build). Phase 5 close entièrement : accueil one-pager complet, aucune page interne clonée, aucun placeholder, aucune coordonnée fictive. |

---

---

## Règle de maintenance

Ce fichier doit être modifié uniquement lorsque les règles de son domaine évoluent.  
Les tâches réalisées, décisions et blocages doivent être consignés dans `10-TRACKING.md`.
