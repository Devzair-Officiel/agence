# Suivi, décisions et changements

> État vivant du projet : tâches, décisions et historique.

## 24. Registre de suivi

| ID | Phase | Tâche | Priorité | Statut | Preuve attendue | Prochaine action |
|---|---|---|---|---|---|---|
| DEV-001 | 0 | Confirmer les services du MVP | Haute | À faire | Liste validée | Cadrage éditorial |
| DEV-002 | 0 | Confirmer la zone desservie | Haute | À faire | Information écrite | Valider les pages locales |
| DEV-003 | 0 | Inventorier les réalisations publiables | Haute | À faire | Dossiers et autorisations | Préparer les études de cas |
| DEV-004 | 1 | Créer Dockerfile.dev | Haute | Terminé | `apps/web/Dockerfile.dev` (Node 24 Alpine, `npm ci` au build) + `.dockerignore` | Écrire un Dockerfile de production en Phase 12 |
| DEV-005 | 1 | Créer compose.yaml | Haute | Terminé | Site accessible sur `http://localhost:3001` (mapping 3001→3000 conteneur), `docker compose config` OK, volumes `web_node_modules` et `web_nuxt_cache` | Ajouter le service `api` (Symfony) en Phase 8 |
| DEV-006 | 1 | Initialiser Git à la racine | Haute | En cours | `.git` racine présent, `.gitignore` posé, un seul `.git` dans l’arbre | Créer le premier commit propre (humain) |
| DEV-007 | 2 | Installer ESLint et le typecheck | Haute | Terminé | `@nuxt/eslint` en devDep, `eslint.config.mjs` en Flat Config (ignore `.nuxt`/`.output`, refuse `no-explicit-any`), `npm run lint` et `npm run typecheck` verts | Étendre les règles au besoin sans surcharger |
| DEV-008 | 2 | Créer l’architecture Nuxt | Haute | Terminé | `app/{assets,components,pages,composables,layouts}` en place, `error.vue` et `app.vue` présents | Créer `server/` et `shared/` quand un besoin réel apparaît (Phase 8+) |
| DEV-009 | 4 | Créer `usePageSeo` | Haute | Terminé | `app/composables/usePageSeo.ts` + utilitaires purs `app/utils/site-url.ts` et `app/utils/canonical.ts` ; `test/unit/composables/usePageSeo.spec.ts` (7 cas indexable/noindex/robots explicite/localhost) et `test/unit/utils/canonical.spec.ts` verts | Migrer les pages Phase 5 au fur et à mesure |
| DEV-010 | 4 | Configurer robots, sitemap et Schema.org | Haute | Terminé | `@nuxtjs/robots` v5.7 + `@nuxtjs/sitemap` v7.6 installés ; `useSiteSchema` injecte Organization + WebSite dans le layout par défaut ; `test/e2e/seo.spec.ts` vert. Curl : `X-Robots-Tag: noindex, nofollow` sur `/`, `robots.txt` : `User-agent: * / Disallow: /`, sitemap XML absolu avec une seule route `/` depuis la clôture Phase 5D | Réévaluer `nuxt-schema-org`, `nuxt-link-checker`, `nuxt-seo-utils` en Phase 9 |
| DEV-011 | 3 | Poser les tokens CSS (couleurs, typos, espacements, radius, ombres, animations, z-index) | Haute | Terminé | `apps/web/app/assets/css/tokens.css` + `global.css` + `animations.css` chargés par Nuxt | Utiliser les tokens dans les pages futures |
| DEV-012 | 3 | Créer les composants de base (BaseContainer, BaseEyebrow, BaseButton, BaseLink) | Haute | Terminé | Composants + tests Vitest (7 cas BaseButton) verts | Étendre au besoin sans surconfigurer |
| DEV-013 | 3 | Créer le header, la nav mobile accessible et le footer | Haute | Terminé | `SiteHeader`, `MobileNavigation` (Teleport, aria-modal, focus trap, Escape, scroll lock, restauration du focus), `SiteFooter`, tests Vitest (10 cas) verts | Ajouter les tests clavier/contrastes et zoom (Phase 3) |
| DEV-014 | 3-5D | Ajouter la page interne `/design-preview` (noindex) | Basse | Terminé | Page rendue en SSR avec `<meta name="robots" content="noindex, nofollow">` | **Retirée en Phase 5D** : coverage utile migrée dans `test/e2e/home-structure.spec.ts`, sitemap n'excluant plus rien, `/design-preview` renvoie 404 (vérifié par `seo.spec.ts`) |
| DEV-015 | 2 | Câbler Vitest 3 + @vue/test-utils + happy-dom, ajouter `vue-tsc` et `typescript` en devDeps, scripts `typecheck` et `test` | Haute | Terminé | `docker compose exec web npm run test` (20 verts), `typecheck` OK, `build` OK | Ajouter ESLint et Playwright plus tard |
| DEV-016 | 1-2 | Clôturer les fondations techniques (Docker reproductible, ESLint Flat Config, Playwright prep, CI GitHub Actions, `@nuxt/fonts`, README, `.env.example`) | Haute | Terminé | `docker compose build` OK, `npm run quality` vert (lint + typecheck + tests 20/20 + build), workflow `.github/workflows/web-quality.yml` en place, smoke test Playwright écrit | Enchaîner sur la Phase 5 (hero + graphe SVG) |
| DEV-017 | 2 | Corriger la régression vue-router R0004 (accueil `/` manquant, `<a>` internes vers routes inexistantes, `@font-face` sur fichiers absents) | Haute | Terminé | Console navigateur propre sur `/design-preview`, page `/` placeholder noindex, prop `external` sur `BaseButton`/`BaseLink` pour les liens vers pages non encore livrées | Retirer `external` au fur et à mesure que les pages sont créées |
| DEV-018 | 3 | Écrire la suite E2E Playwright de clôture Phase 3 (design-preview, mobile-navigation, keyboard-navigation, responsive 390/768/1440, reduced-motion, Axe WCAG 2.2 AA) | Haute | Terminé | 28/28 tests verts dans `mcr.microsoft.com/playwright:v1.62.1-noble` ciblant le conteneur `web` sur `localhost:3001` ; aucune violation Axe `serious`/`critical` sur `/design-preview` | Étendre le scan Axe aux pages marketing en Phase 5 |
| DEV-019 | 3 | Corriger les défauts démontrés par la suite Axe/Playwright | Haute | Terminé | `SiteFooter` : `#5e6b78` → `var(--color-cream-subtle)` (~6:1 sur `--color-ink`) ; `design-preview` : swatch Devzair blue en `color: #000` (≥5.9:1) et `code` en `opacity: 1` | Aucune |
| DEV-020 | 3 | Ajouter le job `e2e` GitHub Actions dans l'image officielle Playwright et exposer `PLAYWRIGHT_BASE_URL` | Haute | Terminé | `.github/workflows/web-quality.yml` : job `e2e` parallèle au job `quality`, image `mcr.microsoft.com/playwright:v1.62.1-noble`, `webServer` conditionnel dans `playwright.config.ts` | Ajouter un job équivalent pour Symfony (Phase 8) |
| DEV-021 | 4 | Étendre `runtimeConfig.public` avec `siteUrl` et `siteIndexable`, mettre à jour `.env.example` et `compose.yaml` | Haute | Terminé | `NUXT_PUBLIC_SITE_URL` et `NUXT_PUBLIC_SITE_INDEXABLE` (défaut `false`) mirroirés dans `site.url` et `site.indexable` ; `.env.example` documente le défaut « safe » | Forcer `NUXT_PUBLIC_SITE_INDEXABLE=true` uniquement en production (Phase 12) |
| DEV-022 | 4 | Injecter Schema.org Organization + WebSite au niveau du layout | Haute | Terminé | `app/composables/useSiteSchema.ts` appelé une fois dans `app/layouts/default.vue` ; JSON-LD présent dans le HTML SSR (curl `/`) ; aucun champ fictif (`aggregateRating`, `address` absents) | Étendre au type `Article` en Phase 9 (à ce moment réévaluer `nuxt-schema-org`) |
| DEV-023 | 4 | Documenter les modules SEO retenus et ceux différés | Moyenne | Terminé | `docs/adr/ADR-004-modules-seo.md` (Option 2 « à la carte », @nuxtjs/robots + @nuxtjs/sitemap seuls, autres modules différés avec justification) | Rouvrir l'ADR à l'ouverture de la Phase 9 |
| DEV-024 | 4 | Créer l'emplacement de l'image OG par défaut sans publier de fichier fictif | Moyenne | Terminé | `apps/web/public/og/.gitkeep` avec instructions de drop-in ; `site.defaultOgImage = null` ; `usePageSeo` n'émet pas de `og:image` tant qu'aucune image n'est fournie | Livrer l'image OG statique validée en Phase 5 |
| DEV-025 | 5A | Livrer la page d'accueil (hero + graphe SVG des cinq pôles) avec SEO réel et pré-rendu | Haute | Terminé | `app/config/expertise-pillars.ts` (contrat typé, 5 pôles), `HomeEcosystemGraph.vue` (SVG accessible role="img" + `<title>`/`<desc>` liés par `aria-labelledby` via `useId()`, animation signature CSS ~1,4 s non bouclée, défense en profondeur `prefers-reduced-motion`), `HomeHero.vue` (eyebrow, H1, CTA vers `#contact`/`#realisations`, réassurance), `pages/index.vue` recâblée sur `usePageSeo({ type: "website" })`, `routeRules['/'] = { prerender: true }`. Curl SSR : `<html lang="fr">`, titre `Agence digitale pour sites web, applications et visibilité \| Devzair`, description validée, canonical `/`, `og:type website`, `twitter:card summary` (pas de logo dispo). Suite étendue : 69 tests unitaires (+17), 51 E2E (+13) verts. Lint, typecheck, build OK | Enchaîner Phase 5B (sections « Le constat », « Réponse globale », « Cinq pôles détaillés ») |
| DEV-026 | 5A | Image Open Graph statique `og/devzair-home.png` (1200×630) pour l'accueil | Moyenne | Bloqué | Aucun logo Devzair présent dans le dépôt (`apps/web/public/` ne contient que `favicon.ico` et `og/.gitkeep`). `defaultOgImage` reste `null`, `usePageSeo` n'émet pas de `og:image` et Twitter retombe sur `summary` (comportement attendu) | Obtenir le logo Devzair (SVG ou PNG haute résolution) auprès du client, puis générer une image 1200×630 alignée sur la charte |
| DEV-027 | 5B | Livrer les trois sections primaires de l'accueil : « Le constat », « La réponse Devzair », « Nos expertises · 5 pôles » | Haute | Terminé | `expertise-pillars.ts` étendu (`longDescription` + `services` + variante `accent`), 3 composants créés (`HomeProblems.vue`, `HomeConnectedApproach.vue`, `HomeExpertisePillars.vue`), `pages/index.vue` orchestre 4 sections. Carrousel mobile CSS-natif (scroll-snap), grille desktop asymétrique 3×2 (Concevoir spans 2 rows, Faire évoluer petrol). Ancre `#expertises` active. 90 tests unitaires (+21), 72 E2E (+21) verts. Curl SSR : 3 H2, 5 `home-problems__number`, 5 `home-approach__step-title`, 5 `home-pillars__card-title`, `id="expertises"` et « Faites défiler » présents. Build : 2.79 MB / 725 kB gzip (+40 KB / +11 KB gzip vs 5A) | Enchaîner Phase 5C (réalisation-témoin, méthode, pourquoi Devzair, CTA final) |
| DEV-028 | 5B | Corriger deux défauts de contraste WCAG démontrés par Axe sur les nouvelles sections | Haute | Terminé | `HomeConnectedApproach.step-index` : Devzair blue #2e86d9 sur navy-elevated #141e2c donnait 4.41:1 (petit texte gras → seuil 4.5:1). Passé sur cream (~13:1). `HomeExpertisePillars` carte accent : Devzair blue sur petrol #0c5b57 donnait 2.08:1. Passé sur cream (~8.8:1) pour index/tag, cream-muted pour la puce ::before | Aucune |
| DEV-029 | 5C | Livrer les trois sections secondaires de l'accueil : « Réalisations » (état honnête), « Notre méthode » (6 étapes), « Pourquoi Devzair » (5 promesses) | Haute | Terminé | 2 configs typées créées (`project-process.ts` 6 étapes, `trust-promises.ts` 5 promesses, texte verbatim brief 5C). 3 composants créés : `HomeFeaturedCaseStudy.vue` (ancre `#realisations`, variante honest-state — aucun faux client), `HomeProcess.vue` (`<ol>` 6 étapes, timeline verticale mobile via `::before`/`::after`, 2 col ≥640px, 3×2 ≥1024px), `HomeTrust.vue` (`<ul>` 5 promesses, 1 col mobile, 2 col+row-span ≥640px, grille 3+2 centrée ≥1024px). `pages/index.vue` orchestre 7 sections. `navigation.ts` : lien header « Réalisations » recablé de `/realisations` (route absente) vers `/#realisations` (ancre active). CTA hero `#realisations` opérationnel sans modification. 120 tests unitaires (+30 : 5+6+7+6+6), 97 E2E (+25 dans `home-sections-secondary.spec.ts`) verts. Curl SSR : 18 signatures éditoriales exactes présentes (H2, honest-state, 6 labels méthode, 5 labels promesses, `id="realisations"`, `/#realisations`). Build : 2.83 MB / 733 kB gzip (+45 KB / +8 KB gzip vs 5B) | Enchaîner Phase 5D (CTA final, ancre de contact temporaire, harmonisation éditoriale, suppression de `/design-preview`) |
| DEV-030 | 5C | Documenter l'absence de projet publiable et la variante « état honnête » retenue pour la section Réalisations | Haute | Terminé | DEV-003 « Inventorier les réalisations publiables » reste `À faire`. Aucune étude de cas Devzair ne satisfait actuellement les exigences de `docs/01-CONTENT.md §7.6` (client autorisé, résultats mesurables, méthode/période de mesure, visuels autorisés). `HomeFeaturedCaseStudy.vue` publie donc eyebrow `Études de cas en préparation` + titre `Nos réalisations détaillées seront bientôt disponibles.` + paragraphe explicatif. Aucun bouton, aucun lien, aucune image. Le jour où DEV-003 est livré, remplacer le bloc `home-case__placeholder` par la variante détaillée sans changer l'ancre ni le titre externe. Cf. DEC-035 | Livrer un premier cas réel dès qu'un projet Devzair aura obtenu autorisation client + résultats vérifiables |
| DEV-031 | 5D | Livrer la section finale CTA `#contact` et clore l'accueil publique (8 sections) | Haute | Terminé | `HomeCallToAction.vue` (section ancrée `#contact`, verbatim `Parlons de votre projet` / `Construisons une présence digitale à la hauteur de votre entreprise.` / paragraphe explicatif). Stratégie de contact conditionnelle (mailto BaseButton si `site.contact.email` défini, sinon rien). Notice preprod discrète visible uniquement si `!contact.email && siteIndexable === false` (source unique `useRuntimeConfig().public.siteIndexable`, jamais `process.env`). `pages/index.vue` orchestre les 8 sections. `primaryCta.to = "/#contact"` (source unique header desktop / footer / menu mobile) ; header nav « Expertises » recablé `/expertises` → `/#expertises`. Tests unitaires : 130 verts (+10 `HomeCallToAction.spec.ts`). Tests E2E : `home-cta-final.spec.ts` (+13) et `home-structure.spec.ts` (+5) ; `home-sections-secondary.spec.ts` étendu à 8 sections. Curl SSR : `id="contact"`, eyebrow, H2, paragraphe et notice preprod visibles avant hydratation | Livrer le formulaire réel (validation, anti-spam, e-mail transactionnel) en Phase 6 |
| DEV-032 | 5D | Supprimer `/design-preview` et migrer sa couverture E2E utile vers `/` | Haute | Terminé | `apps/web/app/pages/design-preview.vue` et `test/e2e/design-preview.spec.ts` supprimés. `test/e2e/home-structure.spec.ts` créé (5 tests : console/router propre, single H1, skip link `#main-content` sur vrai `<main>`, header + footer présents, aucune coordonnée fictive). Sitemap : `exclude` retiré (route inexistante). `test/e2e/seo.spec.ts` vérifie désormais que `/design-preview` renvoie 404. `beforeEach`/`goto('/design-preview')` migrés vers `/` dans `accessibility.spec.ts`, `keyboard-navigation.spec.ts`, `mobile-navigation.spec.ts`, `reduced-motion.spec.ts`, `responsive.spec.ts`. Fixture unitaire `usePageSeo.spec.ts` renommée `/interne-fixture` |
| DEV-033 | 6A | Scaffolder `apps/api` Symfony 7.4 LTS (PHP 8.4, sans DB, sans Doctrine) | Haute | Terminé | `apps/api/{composer.json,Dockerfile.dev,public/index.php,bin/console,src/Kernel.php,config/{bundles.php,services.yaml,services_test.yaml,packages/*.yaml,routes.yaml,preload.php},phpunit.xml.dist,tests/bootstrap.php,.env,.env.test,.gitignore,.dockerignore}`. Kernel `MicroKernelTrait`, bundles minimaux (`FrameworkBundle`, `MonologBundle`). Serializer, Validator, Mailer, RateLimiter, HttpClient, Uid activés | Migration vers FrankenPHP ou PHP-FPM planifiée dès qu'un domaine persistant ou une charge réelle apparaît (cf. ADR-006) |
| DEV-034 | 6A | Implémenter l'endpoint `POST /api/contact` (DTO + controller + service + sender + turnstile + rate limiter + origin allowlist) | Haute | Terminé | `src/Contact/Dto/ContactRequest.php` avec contraintes Validator (email, longueurs, choice, IsTrue consentement, regex téléphone). `src/Contact/Controller/{ContactSubmissionController,HealthController}.php`. `src/Contact/Service/{SubmitContactMessage,ContactMessageSenderInterface,SymfonyContactMessageSender,InMemoryContactMessageSender,ContactSubmissionResult}.php`. `src/Contact/Security/{OriginAllowlist,ContactRateLimiter,TurnstileVerifierInterface,TurnstileVerifierFactory,AlwaysAllowTurnstileVerifier,CloudflareTurnstileVerifier,TurnstileVerdict}.php`. `src/EventListener/ApiExceptionListener.php` convertit toute exception en JSON. Pipeline : Origin (403) → payload > 10 KB (413) → rate limit (429 + Retry-After) → JSON parsing (400) → honeypot silencieux (202) → validation (400) → Turnstile (403) → envoi + réponse 200. `Request-Id` UUID v7 dans réponse + header + logs. Aucun PII dans les logs | Widget Turnstile côté navigateur et formulaire réel en Phase 6B |
| DEV-035 | 6A | Écrire la suite PHPUnit (DTO, endpoint, mailer, logs sans PII, security) | Haute | Terminé | 8 fichiers de test : `ContactRequestValidationTest` (data provider 6 cas invalides), `OriginAllowlistTest` (4 cas), `TurnstileVerifierFactoryTest` (3 cas dont fail-closed sur secret manquant), `CloudflareTurnstileVerifierTest` (4 cas via `MockHttpClient`), `HealthControllerTest`, `ContactSubmissionControllerTest` (7 cas : accepté, Origin absent, Origin refusé, honeypot 202 générique, validation 400, JSON invalide, payload 413, headers réponse), `ContactRateLimitTest` (429 + Retry-After), `SymfonyContactMessageSenderTest` (From app / Reply-To visiteur / X-Request-Id / null recipient throws), `ContactLoggingTest` (aucune PII dans les logs sur happy path). `services_test.yaml` remplace le sender réel par `InMemoryContactMessageSender` | Étendre à la couverture email réel (Symfony Mailer transport) en Phase 6B ou 8 |
| DEV-036 | 6A | Wiring Caddy en frontal (Caddyfile + Dockerfile + compose.yaml) | Haute | Terminé | `infra/caddy/{Caddyfile,Dockerfile}`. Matcher `@api path /api/*` → `reverse_proxy api:8000` + `uri strip_prefix /api`. Fallback → `reverse_proxy web:3000`. `X-Forwarded-*` propagés. `compose.yaml` refactorisé : `caddy` seul service avec port hôte (`3001:80`), `web` et `api` en `expose:` interne seulement. `depends_on: [web, api]`. Volumes `api_vendor`, `api_var` ajoutés. Curl smoke : `curl http://localhost:3001/api/health` → `{"status":"ok"}` | En production, ajouter TLS Caddy automatique et configurer `NUXT_PUBLIC_SITE_URL` sur le domaine réel |
| DEV-037 | 6A | Ajouter le job GitHub Actions `api-quality.yml` pour Symfony | Haute | Terminé | `.github/workflows/api-quality.yml` : PHP 8.4 (shivammathur/setup-php), cache Composer, `composer validate --strict`, `composer install`, `lint:yaml config --parse-tags`, `lint:container --env=test`, `phpunit`. Déclenché sur `apps/api/**` + workflow lui-même, concurrency par ref. Complète le job `web-quality.yml` déjà en place | Ajouter un job d'analyse statique (PHPStan / Psalm) en Phase 8 quand le domaine métier grossit |
| DEV-038 | 6A | Stabiliser Playwright : supprimer les 5 `page.waitForLoadState('networkidle')` et rendre les attentes déterministes | Haute | Terminé | Remplacés dans `mobile-navigation.spec.ts`, `keyboard-navigation.spec.ts` (2 occurrences), `reduced-motion.spec.ts`, `home-cta-final.spec.ts` : `page.goto('/', { waitUntil: 'domcontentloaded' })` + `locator(<élément hydraté>).waitFor({ state: 'visible' })`. CI garde `workers: 1`, `retries: 1`, build Nitro préalable. Reporter CI enrichi : `[['line'], ['github'], ['html', ...]]` pour synthèse passed/flaky/failed en stdout | Ajouter un job matrix Firefox/WebKit uniquement quand la couverture visuelle multi-navigateur devient nécessaire |
| DEV-039 | 6A | Documenter Phase 6A (ADR-006 + ADR-007 + README + apps/api/README + 05/06/07/08) | Haute | Terminé | `docs/adr/ADR-006-runtime-symfony-caddy.md` (runtime PHP built-in server + Caddy, PostgreSQL différé), `docs/adr/ADR-007-endpoint-contact-securite.md` (CSRF Option B, Turnstile, rate limit, logging sans PII, table des réponses HTTP). `README.md` racine mis à jour (architecture 3 services, démarrage curl, variables backend). `apps/api/README.md` créé (stack, pipeline, dev local, contrôles qualité, table des variables). `docs/05-SECURITY-PRIVACY.md` §14.9 et §14.11 étendus. `docs/06-ARCHITECTURE-CODE.md` §16.1, §16.2, §16.10 mis à jour (Phase 6A vs différé, ADR renumérotés). `docs/07-QUALITY-DELIVERY.md` §20 : table des jobs CI actifs + note Playwright | Étendre la note d'incident dès qu'un premier abus réel est constaté sur `/api/contact` (Phase 6B ou 7) | Aucune |
| DEV-040 | 6B | Livrer le formulaire de contact accessible + widget Turnstile + intégration dans la section `#contact` de l'accueil | Haute | Terminé | Domaine `app/components/contact/` (4 composants) + `app/composables/useContactForm.ts` + `app/types/contact.ts`. `ContactForm.vue` orchestrateur : `<form novalidate aria-labelledby aria-describedby>`, fieldset radiogroup `projectType`, honeypot `website` hors flux + `aria-hidden` + `tabindex=-1`, notice RGPD liée par aria-describedby, focus management (bandeau succès `role="status"` / erreur `role="alert"` + premier champ invalide sur validation). Composable `useContactForm` sans coupleur Nuxt (testable avec `fetch` stubé, cf. DEC-052) : validation client miroir non-strict du DTO Symfony, mapping HTTP 200/202/400/403/413/429 → codes ADR-007 avec Retry-After parsé, protection double-clic. `TurnstileWidget.vue` client-only avec fallback dev-noop quand `NUXT_PUBLIC_TURNSTILE_SITE_KEY` est vide (cf. DEC-053). `HomeCallToAction.vue` : suppression du mailto conditionnel et de la notice preprod (placeholders Phase 5D). Runtime config étendue (`turnstileSiteKey` dans `nuxt.config.ts`, `.env.example`, `compose.yaml`). Tests : 154 unitaires (+24) verts dont 20 `useContactForm` (validation + mapping HTTP + double-submit) et 6 `ContactForm` (structure, honeypot, ARIA). 117 E2E verts dont `contact-form.spec.ts` (SSR, happy path dev-noop, HTTP 400 par champ, HTTP 429 avec Retry-After, honeypot transmis, validation client bloque une soumission vide, Axe WCAG 2.2 AA, prefers-reduced-motion — backend mocké via `page.route`) et `home-cta-final.spec.ts` réaligné (form présent + assertions preprod/mailto retirées). PHPUnit backend intact : 31/31. Build : 2.85 MB / 740 kB gzip (+50 KB / +12 KB gzip vs 2.10, dans le budget +80 KB / +20 KB) | Brancher un site-key Turnstile réel + `MAILER_DSN` production en Phase 12 ; capturer les 429 réels via le canal Monolog `contact` pour ajuster le token bucket |
| DEV-042 | 7A | Livrer l'architecture éditoriale et les pages institutionnelles `/agence` + `/expertises` (Phase 7A) | Haute | Terminé | Config typée `app/config/expertise-pages.ts` (5 pages `status: "planned"`, aucune route générée). 4 composants éditoriaux réutilisables : `EditorialHero.vue`, `EditorialSection.vue` (variantes `default`/`subtle`/`inverse`, `aria-labelledby` via `useId`), `EditorialCallout.vue` (variantes `inverse`/`accent`, `primaryCta` + `secondaryCta` facultatif), `ExpertiseOverviewCard.vue` (`<article>`, H3 non lié, badge d'ordre `aria-hidden`, aucun `<a>` en 7A). 2 pages : `pages/agence.vue` (H1 verbatim, eyebrow `L'agence`, 3 sections Positionnement/Fonctionnement/Valeurs, callout vers `/expertises`) et `pages/expertises/index.vue` (H1 verbatim, eyebrow `Nos expertises`, section approche, grille de 5 `ExpertiseOverviewCard` alimentée par `expertisePillars`, callout vers `/agence`). Pré-rendu ajouté (`routeRules['/agence'|'/expertises']={prerender:true}`). Navigation refondue : `navigation.ts` gagne `isRoute: boolean` par entrée pour piloter `NuxtLink :external` (fin des warnings vue-router R0004 sur les ancres) ; `primaryNavigation` = Expertises `/expertises` + Agence `/agence` + Réalisations `/#realisations` (liens morts `Méthode`, `Ressources` supprimés) ; footer réduit au groupe `Découvrir` (3 entrées) ; `legalNavigation = []`. `SiteHeader.vue`/`SiteFooter.vue`/`MobileNavigation.vue` mis à jour pour respecter `isRoute`. Tests : 213/213 unitaires verts (+51 : `expertise-pages` ×11, `EditorialHero` ×7, `EditorialSection` ×9, `EditorialCallout` ×7, `ExpertiseOverviewCard` ×6, page `/agence` ×6, page `/expertises` ×7). Nouvelle suite E2E `test/e2e/institutional-pages.spec.ts` (18 cas : SSR HTML, unique H1 verbatim, eyebrow + intro en SSR, SEO complet — title/description/canonical *ou* noindex/`og:url` —, lien réel `/#contact`, absence de lien vers `/expertises/{slug}`, 5 cartes sans `<a>`, navigation principale exposant Agence + Expertises + Réalisations + CTA `/#contact`, maillage `/agence` ↔ `/expertises`, responsive 390/768/1440, Axe WCAG 2.2 AA, `prefers-reduced-motion`). SSR vérifié sur `.output/public/{agence,expertises}/index.html`. Build : 2.92 MB / 759 kB gzip (+70 KB / +18 KB vs 6C 2.85 MB / 741 kB). Lint + typecheck + build verts | Enchaîner Phase 7B (routes `/expertises/{slug}` — 5 pages détaillées, contenu à valider) : basculer `expertise-pages.status` `planned` → `live` uniquement à la publication réelle, transformer les cartes en liens (H3 → `NuxtLink`), ajouter Schema.org `Service`. |
| DEV-041 | 6C | Préparer la mise en production du transport SMTP OVHcloud : `MAILER_DSN` comme seule variable d'entrée, Turnstile facultatif via deux flags alignés, HTTP 503 `temporary_error` sur échec SMTP, service de diagnostic reproductible | Haute | Terminé | Nouveau domaine backend `App\Contact\Configuration\` : `ContactConfigurationIssue` (VO readonly, `error()`/`warning()`), `ContactConfigurationReport` (liste d'issues, `isValid()`), `ContactConfigurationValidator` (service pur : `MAILER_DSN` non vide + non `null://null` en prod + non plaintext en prod, `CONTACT_RECIPIENT`/`CONTACT_FROM_EMAIL` présents et valides, `CONTACT_FROM_NAME` non vide, Turnstile activé ⇒ secret présent, Turnstile désactivé en prod ⇒ warning, `CONTACT_ORIGIN_ALLOWLIST` non vide, `TRUSTED_PROXIES` warning si vide ; ne divulgue jamais DSN complet, secret ni email en clair). Nouvelle commande `bin/console app:contact:check` (`#[AsCommand]`, SymfonyStyle tables error/warning, exit `0`/`1`). Marker de domaine `ContactTemporarilyUnavailableException`. `SymfonyContactMessageSender` capture `TransportExceptionInterface` et re-lève l'exception marker (chaîne l'origine pour debug interne uniquement). Contrôleur : nouveau `ContactSubmissionResult::STATUS_TEMPORARY_ERROR` → HTTP 503 + `{"status":"error","code":"temporary_error","request_id":"…"}` + `Cache-Control: no-store` + `X-Request-Id`. Log `contact.mailer_unavailable` (canal `contact`, warning, aucun PII). `services.yaml` étendu (params `app.mailer.dsn`, `app.trusted_proxies` + wiring validateur). `InMemoryContactMessageSender` étendu (`failNextWith`/`failNextTemporarily` — seam pour tests). Front : `NUXT_PUBLIC_TURNSTILE_ENABLED` (défaut `false`) ajouté à `runtimeConfig.public`, `nuxt.config.ts`, `.env.example`, `compose.yaml` ; `TurnstileWidget.vue` gagne prop `enabled` (dev-noop si `!enabled || !siteKey`) ; `ContactForm.vue` gagne prop `turnstileEnabled` et bandeau verbatim « Service momentanément indisponible » / « Le service est momentanément indisponible. Votre message n'a pas été envoyé. Merci de réessayer plus tard. » sur code `temporary_error` ; `HomeCallToAction.vue` lit `runtimeConfig.public.turnstileEnabled` ; `useContactForm` mappe HTTP 503 → `temporary_error` (avec fallback `if httpStatus === 503`), `types/contact.ts` ajoute `'temporary_error'` à `ContactErrorCode`. Tests : PHPUnit +17 (`ContactConfigurationValidatorTest` 14 cas, `ContactCheckCommandTest` 3 cas, +1 test mailer transport-exception, +1 test controller 503, +1 test logging 503 sans PII). Vitest +2 (`useContactForm` HTTP 503 + valeurs préservées, `ContactForm` bandeau 503 + valeurs préservées). Playwright +2 (503 bandeau + valeurs préservées, Turnstile désactivé → 0 requête Cloudflare + 0 script Cloudflare + notice dev visible). Docs : ADR-008 (`docs/adr/ADR-008-mailer-ovhcloud-turnstile-optionnel.md`), checklist opérationnelle (`docs/checklists/PRODUCTION-CONTACT.md`, 10 sections), READMEs (racine + `apps/api` + `apps/web`), `.env.example`, `05/06/07/08` mis à jour | Exécuter la checklist `docs/checklists/PRODUCTION-CONTACT.md` lors de la mise en prod OVHcloud réelle (Phase 12) ; capturer les premiers `contact.mailer_unavailable` pour dimensionner un éventuel worker Messenger + retry disque |
| DEV-043 | 7B | Livrer les 5 pages d'expertise détaillées `/expertises/{slug}` avec maillage SEO, Schema.org Service et 404 explicite | Haute | Terminé | Contrat `app/config/expertise-pages.ts` étendu de 11 nouveaux champs `readonly` (`eyebrow`, `introduction`, `seoTitle`, `seoDescription`, `needTitle`, `needDescription`, `approachTitle`, `approachDescription`, `deliverables[]`, `benefits: ExpertiseBenefit[]`, `relatedPillarIds[]`) + nouveau type `ExpertiseBenefit`. Les 5 entrées passent en `status: "published"` avec contenu éditorial verbatim (concevoir, construire, valoriser, visibilite, faire-evoluer). Route dynamique `app/pages/expertises/[slug].vue` : résolution slug via `expertisePages.find(p => p.slug === slug && p.status === "published")`, `createError({ statusCode: 404, fatal: true })` sur slug inconnu (aucun fallback silencieux), orchestre `SiteBreadcrumb` + `ExpertisePageHero` + 4 `EditorialSection` (À qui cela s'adresse / Notre approche / Livrables / Bénéfices) + `ExpertiseRelatedPillars` + `EditorialCallout` retour vers `/expertises`. Nouveau composable `app/composables/useExpertiseServiceSchema.ts` : JSON-LD `@type: Service`, `@id: ${url}#service`, `provider: { "@id": ${origin}/#organization }` (référence l'`@id` de `useSiteSchema`), `serviceType: shortTitle` ; aucun `offers`/`price`/`aggregateRating`/`review`. Composants livrés : `SiteBreadcrumb.vue` (`<nav aria-label="Fil d'Ariane">`, `<ol role="list">`, séparateur `›` `aria-hidden`, dernier item `aria-current="page"`), `ExpertisePageHero.vue` (H1 unique + eyebrow + introduction), `ExpertiseDeliverables.vue` (`<ul role="list">` pastilles + marqueurs `aria-hidden`), `ExpertiseBenefits.vue` (grille de bénéfices H3+description), `ExpertiseRelatedPillars.vue` (2 cartes-liens, filtrage `status === "published"`, CTA en `--color-petrol` — contraste WCAG AA ≥ 6:1). `ExpertiseOverviewCard.vue` : refactor en carte cliquable conditionnelle avec `<NuxtLink v-if="isPublished()">` + `<article v-else>` (pas de `<component :is>` qui laisserait un `<nuxtlink>` custom element non résolu à l'hydratation — cf. DEC-062). `pages/expertises/index.vue` joint `expertisePillars` × `expertisePages` via `pagesByPillarId` (Map computed). `nuxt.config.ts routeRules` étendu : 5 nouvelles routes pré-rendues (`/expertises/concevoir`, `/construire`, `/valoriser`, `/visibilite`, `/faire-evoluer`) — vérifié sur `.output/public/expertises/{slug}/index.html`, sitemap XML mis à jour automatiquement. Tests unitaires : 258/258 verts (+45) — `expertise-pages` étendu (+9 assertions groupées), `SiteBreadcrumb` ×6, `ExpertisePageHero` ×5, `ExpertiseDeliverables` ×4, `ExpertiseBenefits` ×5, `ExpertiseRelatedPillars` ×5, `useExpertiseServiceSchema` ×5, `pages/expertise-slug` ×7 (mount dynamique par slug via `vi.resetModules()` + `await import`). Nouvelle suite E2E `test/e2e/expertise-pages.spec.ts` (SSR 200 par slug, HTTP 404 sur slug inconnu, sitemap XML, JSON-LD Service, fil d'Ariane, absence de placeholder/lorem/TODO, 2 liens de pôles connexes, mobile-nav, responsive 320/390/768/1024/1440, Axe WCAG 2.2 AA, `prefers-reduced-motion`) + `institutional-pages.spec.ts` réaligné (5 liens vers pages détaillées désormais attendus). Playwright complet : **200/200 verts, 0 flaky, 0 failed** (`--workers=1 --retries=0`). Régex tolérante à l'encodage HTML des apostrophes (`'` ⇔ `&#39;` ⇔ `&#x27;`) dans les assertions SSR. Build : 3.00 MB / 782 kB gzip (+80 KB / +23 KB vs 7A 2.92 MB / 759 kB) — 8 pages pré-rendues (`/`, `/agence`, `/expertises`, 5 × `/expertises/{slug}`) | Enchaîner Phase 7C (études de cas réelles) uniquement après livraison de DEV-003 (inventaire des projets publiables + autorisations client). D'ici là, la section `#realisations` de l'accueil garde sa variante « état honnête » (cf. DEC-035) |

### Statuts autorisés

- `À faire`
- `En cours`
- `Bloqué`
- `En revue`
- `Terminé`
- `Abandonné`

---

## 25. Journal des décisions

| Date | ID | Décision | Raisons | Conséquences |
|---|---|---|---|---|
| 2026-08-01 | DEC-001 | Présenter Devzair comme une agence digitale à taille humaine | Refléter l’offre globale et le fonctionnement projet | Employer « nous », ne pas utiliser le positionnement freelance |
| 2026-08-01 | DEC-002 | Utiliser Nuxt 4 avec Vue et TypeScript strict | SSR, routage, métadonnées et maintenabilité | Ne pas construire une SPA Vue pure |
| 2026-08-01 | DEC-003 | Utiliser un monorepo Docker Compose | Préparer l’ajout de Symfony sans restructuration | Git unique à la racine |
| 2026-08-01 | DEC-004 | Utiliser Symfony 7.4 LTS pour l’API future | Stabilité et maintenance longue | API ajoutée dans `apps/api` |
| 2026-08-01 | DEC-005 | Appliquer SOLID de façon pragmatique | Séparer les responsabilités sans surarchitecture | Pages, composants, composables, services et repositories distincts |
| 2026-08-01 | DEC-006 | Centraliser les métadonnées communes | Éviter les incohérences et répétitions | Création de `usePageSeo` |
| 2026-08-01 | DEC-007 | Pré-rendre les pages marketing et garder le SSR pour le blog initial | Performance et fraîcheur | Cache dynamique décidé plus tard |
| 2026-08-01 | DEC-008 | Ne pas installer de modules pendant le scaffolding | Base maîtrisée | Modules ajoutés avec contrôle et tests |
| 2026-08-01 | DEC-009 | Utiliser des imports explicites (Vue, composables, composants layout) plutôt que les auto-imports Nuxt dans les composants testés | Vitest n'exécute pas la transformation d'auto-import : dépendre de celle-ci masque les erreurs et rend la suite fragile | Chaque composant sous test importe explicitement ses dépendances, `~/composables` et `~/components/layout` |
| 2026-08-01 | DEC-010 | Refs de module (pas `useState`) pour `useMobileNavigation` | Le menu mobile est un singleton par onglet ; l'hydratation SSR n'a pas besoin d'être partagée par clé, et `useState` couple le composable au runtime Nuxt (donc à Vitest) | Composable pur, testable sans mocks Nuxt |
| 2026-08-01 | DEC-011 | Épingler `typescript` à `~5.9.0` | `vue-tsc` 3.3 ne supporte pas encore les changements d'exports internes de TypeScript 7 (`./lib/tsc`) | À revoir quand `vue-tsc` publie une version compatible TS 7 |
| 2026-08-01 | DEC-012 | Ne pas publier de coordonnées avant validation | Règle 1 (rien inventer) ; `site.contact.{email,phone,city}` = `null` | Le footer ne rend rien pour les coordonnées absentes |
| 2026-08-02 | DEC-013 | Installer les dépendances **au build Docker** (`npm ci` dans `Dockerfile.dev`) et exposer `node_modules` via un volume nommé | Éviter un `npm install` à chaque `docker compose up` (lent, sensible aux différences d’UID hôte, source de désync `package-lock`) | Rebuild explicite requis quand `package-lock.json` change |
| 2026-08-02 | DEC-014 | Rester en `USER root` dans le Dockerfile de dev | Compatibilité avec le bind mount `./apps/web:/app` (UID hôte différent selon la machine) | Le Dockerfile de production, à écrire en Phase 12, utilisera `USER node` |
| 2026-08-02 | DEC-015 | Utiliser Flat Config pour ESLint via `@nuxt/eslint` | Format supporté officiellement par Nuxt 4 et ESLint 10 ; permet d’ajouter nos règles sans casser la base auto-générée | Toute règle projet vit dans `apps/web/eslint.config.mjs` (surcouche de `./.nuxt/eslint.config.mjs`) |
| 2026-08-02 | DEC-016 | Choisir `@nuxt/fonts` pour l’auto-hébergement des polices | Détecte les `font-family` du CSS, télécharge au build, sert depuis notre origine, injecte un fallback métrique (size-adjust) pour limiter le CLS. Aucun CDN tiers en runtime | Les fichiers woff2 manuels de `/public/fonts/` deviennent inutiles ; on ne référence plus de fichier absent |
| 2026-08-02 | DEC-017 | Prop `external` sur `BaseButton` et `BaseLink` pour les liens vers pages non encore livrées | `NuxtLink` interne appelle `router.resolve` et émet `[VUE_ROUTER_R0004] No match found` pour toute route inexistante ; `external` force un `<a href>` neutre | Retirer la prop à mesure que les pages cibles sont créées (Phase 5) |
| 2026-08-02 | DEC-018 | CI GitHub Actions déclenchée uniquement sur changements de `apps/web/**` ou du workflow lui-même | Éviter les runs superflus pour de la doc pure ; garder le feedback rapide (~5-10 min) | Ajouter un workflow séparé pour Symfony en Phase 8 |
| 2026-08-02 | DEC-019 | Exécuter Playwright dans l'image officielle `mcr.microsoft.com/playwright:v1.62.1-noble`, hors du conteneur `web` (Alpine) | Les binaires Chromium fournis par Playwright ne tournent pas sur Alpine ; l'image officielle embarque Chromium + toutes les libs système, et est immuable par version. Solution A du brief Phase 3 | En local : soit sur la machine hôte après `npx playwright install`, soit via `docker run` sur l'image officielle en pointant `PLAYWRIGHT_BASE_URL` vers `localhost:3001`. En CI : job `e2e` dédié, parallèle au job `quality` |
| 2026-08-02 | DEC-020 | Forcer `await page.emulateMedia({ reducedMotion: 'reduce' })` dans le `beforeEach` de `reduced-motion.spec.ts` au lieu de compter sur `test.use({ reducedMotion: 'reduce' })` | Dans notre configuration Playwright 1.62 + Chromium, `test.use` ne propage pas toujours l'option au bon moment de l'hydratation Vue ; `matchMedia('(prefers-reduced-motion: reduce)').matches` restait `false` au premier assert | Systématiser `page.emulateMedia` pour tout test dépendant d'une media-query |
| 2026-08-02 | DEC-021 | Retenir uniquement `@nuxtjs/robots` et `@nuxtjs/sitemap` du bundle Nuxt SEO (Option 2 « à la carte »), gérer Schema.org à la main | Deux entités Schema.org stables (Organization + WebSite) ne justifient pas `nuxt-schema-org` ; `nuxt-link-checker` et `nuxt-seo-utils` n'ont aucun bénéfice sur 2 routes réelles. Cf. ADR-004 | Réévaluer à l'ouverture de la Phase 9 (blog, Article, BreadcrumbList) |
| 2026-08-02 | DEC-022 | `NUXT_PUBLIC_SITE_INDEXABLE` = `false` par défaut, seule source de vérité pour toute la politique d'indexation | La preprod ne doit jamais devenir indexable par simple oubli d'une variable ; mirroir unique vers `runtimeConfig.public`, `site.indexable`, `@nuxtjs/robots` | La production doit explicitement forcer `true` — à documenter dans la recette Phase 12 |
| 2026-08-02 | DEC-023 | Une page `noindex` n'émet **pas** de balise canonical | Un canonical sur une page noindex renvoie un signal contradictoire à un moteur qui l'aurait crawlée ; mieux vaut n'émettre aucun signal d'URL préférée | Test unitaire dédié (`usePageSeo — page noindex`) et test E2E `/design-preview reste noindex sans canonical` |
| 2026-08-02 | DEC-024 | Le canonical est calculé exclusivement à partir de `NUXT_PUBLIC_SITE_URL` + `path`, jamais depuis le header HTTP Host | Le header Host peut être manipulé (proxy mal configuré, attaque) et diffère selon l'environnement (Docker interne 3000 vs public 3001) ; la source doit être immuable et contrôlée par le déploiement | `buildCanonical()` accepte uniquement `siteUrl` + `path` ; commentaire explicite dans `app/utils/site-url.ts` |
| 2026-08-02 | DEC-025 | Différer la création de l'image Open Graph par défaut plutôt que publier un placeholder | Règle 1 (ne rien inventer) : une image OG représente l'agence ; sans validation client elle ne doit pas apparaître dans le HTML | `apps/web/public/og/.gitkeep` documente l'emplacement ; `defaultOgImage = null` ; à livrer en Phase 5 |
| 2026-08-02 | DEC-026 | Différer le pré-rendu de `/` en Phase 5 | La page d'accueil actuelle est un placeholder noindex ; la pré-rendre figerait un contenu volatile | `routeRules` vide, commentaire dans `nuxt.config.ts` ; à activer quand la vraie home Phase 5 sera livrée |
| 2026-08-02 | DEC-027 | Rendre le graphe SVG accessible par `role="img"` + `<title>`/`<desc>` liés via `aria-labelledby`, plutôt que par une liste ARIA de nœuds | Le graphe est informatif, pas interactif : une seule annonce (« Les cinq pôles… ») + une description longue suffisent. Une liste ARIA de nœuds serait doublement lue avec la version texte de la page (règle éditoriale : les 5 pôles réapparaissent en Phase 5B) | Les sous-groupes graphiques sont `aria-hidden="true"`, aucun `tabindex` n'est posé, les ids `aria-labelledby` sont générés par `useId()` (SSR-safe, aucune collision si le composant est rendu plusieurs fois) |
| 2026-08-02 | DEC-028 | Animation signature en CSS pure (stroke-dasharray + opacity + transform), non bouclée, ~1,4 s | Pas de dépendance d'animation, pas de JS, `prefers-reduced-motion` déjà géré par `animations.css` (règle globale `!important` sur `animation-duration`) — mais celle-ci ne rétablit pas les états initiaux (`stroke-dashoffset:200`, `opacity:0`). Une règle locale de défense en profondeur force l'état final | Ajouter des animations « décoratives » suit le même pattern : durée finie, `animation-fill-mode: forwards`, media-query locale forçant l'état final |
| 2026-08-02 | DEC-029 | Séparer les positions graphiques (x/y/labelAnchor/labelDx/labelDy) et les données métier (label, description, order, variant) des cinq pôles | Une position (240, 75) n'a de sens que dans un SVG 480×480 ; l'exposer via `expertisePillars` polluerait un contrat qui doit rester consommable par la future page `/expertises`, la grille Phase 5B et un éventuel export IA | `expertisePillars` reste une source unique de vérité pour label/description/ordre ; le tableau `positions` local à `HomeEcosystemGraph.vue` est le point d'extension pour toute nouvelle mise en page graphique |
| 2026-08-02 | DEC-030 | Les CTA de l'accueil pointent vers les ancres `#contact` et `#realisations` tant que les pages/sections cibles n'existent pas | Interdit d'introduire des routes vides ou de sortir l'accueil vers un placeholder ; l'ancre reste utilisable (au pire, no-op) et remplaçable en une ligne quand la vraie cible sera livrée | Retirer `external` et remplacer `to="#contact"` par `to="/contact"` dès que la page `/contact` est publiée (Phase 6) ; idem pour `/realisations` (Phase 7) |
| 2026-08-02 | DEC-031 | Étendre `expertise-pillars.ts` avec `longDescription` et `services` plutôt que renommer `description` (qui reste consommée par HomeEcosystemGraph pour le libellé court dans le SVG) | Le SVG du hero affiche « design · UX · identité » comme sous-titre court, la grille détaillée a besoin d'une phrase narrative et de 3 livrables. Renommer aurait cassé le SVG sans bénéfice. Le contrat garde une seule source de vérité pour chaque niveau de granularité | Toute nouvelle vue des pôles (page `/expertises`, exports IA…) doit préférer `longDescription` pour un discours narratif, `description` pour un libellé court en contexte contraint |
| 2026-08-02 | DEC-032 | Ajouter une variante `accent` (Faire évoluer) à côté des variantes existantes `primary` (Construire) et `default` | `HomeExpertisePillars` a besoin d'un fond petrol distinct sur le dernier pôle pour marquer la clôture du parcours. Le SVG hero, lui, ne check que `variant === 'primary'` : tout ce qui n'est pas primary rend en navy-elevated → l'ajout est rétro-compatible sans toucher au SVG | Toute nouvelle vue peut consommer les 3 variantes ; ne pas confondre `primary` (accent hero) et `accent` (accent grille) — ce sont deux emphases indépendantes |
| 2026-08-02 | DEC-033 | Carrousel mobile CSS-natif (`scroll-snap-type: x mandatory`) plutôt qu'une bibliothèque JS | Aucune dépendance ajoutée, fonctionne sans JS (SSR + progressive enhancement), pas de piège d'accessibilité (les 5 cartes sont toujours dans le DOM, indexables, lisibles au lecteur d'écran, sans bouton fantôme prev/next). Coût nul en bundle | Réévaluer uniquement si un besoin métier réel apparaît (autoplay, tabs synchronisés, animation contrôlée) — sinon rester natif |
| 2026-08-02 | DEC-034 | Sur fond petrol ou navy-elevated, ne pas utiliser Devzair-blue (`#2e86d9`) pour du petit texte gras | Contraste WCAG 2.2 AA insuffisant : 2.08:1 sur petrol, 4.41:1 sur navy-elevated (seuil 4.5:1 pour petit texte gras). Documenté par Axe rule `color-contrast`. Cf. DEV-028 | Utiliser cream / cream-muted pour les tags/index sur ces surfaces. Devzair-blue reste utilisable comme accent décoratif sur navy de base (7:1+) et sur cream/sand (5:1+) |
| 2026-08-02 | DEC-035 | Publier la section « Réalisations » en variante « état honnête » plutôt qu'avec un cas fictif, un logo générique ou un skeleton loader | Règle 1 (AGENTS.md, DEC-001) interdit d'inventer un client, un chiffre ou une preuve. DEV-003 n'est pas encore livré. L'ancre `#realisations` doit néanmoins exister (le CTA hero et la nav header y pointent), donc la section est présente avec un titre externe permanent (« Des solutions concrètes… ») et un placeholder honnête interne | Le jour où un cas réel est publié, remplacer le bloc `home-case__placeholder` par la variante détaillée sans casser l'ancre. Ne jamais introduire de « logo cloud » ou de témoignage anonyme pour combler l'intervalle |
| 2026-08-02 | DEC-036 | Le lien header « Réalisations » cible `/#realisations` (ancre same-page) plutôt qu'une route `/realisations` non livrée | La page dédiée n'existe pas encore (Phase 7). Pointer vers une route absente casse la nav et rompt la promesse « pas de route vide » (DEC-017). L'ancre est active, la section existe (même en variante honnête), l'utilisateur atterrit sur un contenu | Migrer vers `/realisations` uniquement quand la page dédiée sera livrée (Phase 7). Même logique appliquée si d'autres sections one-pager deviennent des routes plus tard |
| 2026-08-02 | DEC-037 | Six colonnes pour la méthode desktop refusées au profit d'une grille 3×2 dès 1024px | Six colonnes contenant un H3 + un paragraphe de ~35 mots à 1024px produisent un texte serré et une hiérarchie visuelle floue. Le brief 5C §9 autorise explicitement le fallback 3×2. La lisibilité prime sur la fidélité littérale à un layout 6 colonnes | Réévaluer uniquement si le contenu devient significativement plus court (titre + 1 phrase max) ; garder 3×2 sinon |
| 2026-08-02 | DEC-038 | Publier `HomeCallToAction` sans bouton tant que `site.contact.email` est `null`, plutôt qu'un mailto vide, un `href="#"` ou un bouton désactivé | Règle AGENTS.md #1 « ne jamais inventer un moyen de contact » ; un bouton non fonctionnel dégrade la confiance et peut ouvrir le client mail sur une adresse vide (mailto:) ou déclencher un scroll parasite (`#`). La section garde son ancrage `#contact` opérationnel — seul le CTA est conditionnel | Ajouter un mailto BaseButton automatiquement dès que `site.contact.email` sera défini. En Phase 6, remplacer le bloc par le vrai formulaire (validation serveur, anti-spam) |
| 2026-08-02 | DEC-039 | Publier la notice « Le moyen de contact en ligne sera activé avant la mise en production. » uniquement si `!contact.email && siteIndexable === false` | Cette notice rassure un lecteur interne (preprod) mais divulgue un détail d'implémentation qui n'a rien à faire en production. Le double garde-fou (contact absent **et** env non indexable) empêche toute fuite si la production reste transitoirement sans coordonnée | Retirer automatiquement dès que `site.contact.email` est défini, ou en production (`NUXT_PUBLIC_SITE_INDEXABLE=true`). Aucune modification du composant nécessaire |
| 2026-08-02 | DEC-040 | Lire l'indexabilité dans un composant via `useRuntimeConfig().public.siteIndexable`, jamais `process.env` | `process.env` n'est pas défini côté client Nuxt et bypasserait la source unique posée par DEC-022. `runtimeConfig.public` est mirrorée à partir des variables `NUXT_PUBLIC_*` par Nuxt lui-même, testable via un stub (`test/setup.ts` installe un défaut, chaque test peut le surcharger) | Toute lecture d'une variable publique dans un composant doit passer par `useRuntimeConfig().public.*` — vérifier au review |
| 2026-08-02 | DEC-041 | Supprimer `/design-preview` à la clôture Phase 5D plutôt que la conserver en interne noindex | La page a rempli son rôle d'atelier design system pendant Phases 3 et 4 : Axe, contrôles clavier, contrastes. Depuis Phase 5, l'accueil réelle expose les mêmes composants dans leur contexte de production, avec plus de surface (8 sections vs 2 blocs de démo). Conserver `/design-preview` doublerait la maintenance sans bénéfice — et la sortie d'un noindex par erreur exposerait un contenu de démo. La coverage utile a été migrée dans `test/e2e/home-structure.spec.ts` | Aucun besoin de page interne pour les futurs lots ; les composants isolés sont couverts par Vitest (props, variantes), l'intégration par les E2E sur `/` |
| 2026-08-02 | DEC-042 | Retenir PHP 8.4 CLI `php -S` pour la Phase 6A plutôt que FrankenPHP ou PHP-FPM/Nginx | Phase 6A : zéro DB, un endpoint POST + un endpoint GET, aucune session, aucun asset servi par PHP. `php -S` derrière Caddy suffit, se documente en une ligne dans le Dockerfile, ne réclame ni pool ni supervisord, et permet un `bin/console` reproductible en dev. Documenté dans ADR-006 | Migrer vers FrankenPHP (worker) ou PHP-FPM dès qu'un domaine persistant (PostgreSQL/Doctrine), une charge réelle (>QPS quotidien mesurable) ou un besoin de serveur d'assets apparaît. Déclencheurs listés en ADR-006 |
| 2026-08-02 | DEC-043 | Adopter la CSRF « Option B » stateless (allowlist stricte de l'`Origin` + Turnstile + rate limit) au lieu d'un jeton CSRF par session | Le site Nuxt est majoritairement pré-rendu ; introduire une session pour émettre un jeton CSRF casserait le prerender et ajouterait un cookie inutile. La combinaison Origin (rejette les POST cross-site classiques), Turnstile (rejette les bots headless) et rate limiter (limite les abus) offre une défense en profondeur adaptée à un formulaire de contact. Documenté dans ADR-007 | Toute requête sans `Origin` correspondant à `CONTACT_ORIGIN_ALLOWLIST` est refusée en 403 `origin_not_allowed`. Ajouter une origine → l'ajouter à la variable d'environnement (jamais dans le code). Si un espace client authentifié est introduit plus tard, réévaluer et repasser à un jeton par session pour ce périmètre |
| 2026-08-02 | DEC-044 | `MAILER_DSN=null://null` par défaut, aucun email envoyé tant que la boîte réelle n'est pas branchée | Un défaut d'envoi bloqué par configuration est infiniment moins grave qu'un email involontaire (dev qui atteint la boîte prod, spam de test à la clôture d'un CI). Le NullTransport de Symfony consomme l'`Email` sans erreur, donc l'endpoint continue de répondre 200 et les tests d'intégration passent | Documenter la variable dans `.env.example` et dans `README.md`. En production, changer explicitement `MAILER_DSN` et surveiller les premiers envois via le log `contact` (canal dédié) — `Request-Id` permet le rapprochement |
| 2026-08-02 | DEC-045 | Aucun PII dans les logs de l'endpoint contact (ni message, ni email, ni téléphone, ni token Turnstile, ni secret) | Un log est copié sur disque, roulé, potentiellement exporté vers un agrégateur externe : c'est un canal à faible contrôle. Le RGPD exige la minimisation (docs/05 §15) et un incident sur les logs est un incident sur des données personnelles. Le `Request-Id` (UUID v7 dans le log + réponse + header `X-Request-Id`) permet le support sans exposer le contenu | Le test `ContactLoggingTest` échoue si une valeur PII marquée (`SECRET_*`) apparaît dans les records Monolog sur le happy path. Ne pas ajouter de nouveau `logger->info()` dans le domaine contact sans passer par les mêmes canaux + tester la non-fuite |
| 2026-08-02 | DEC-046 | `OriginAllowlist` normalise le trailing slash (`https://foo.com/` ≡ `https://foo.com`) et refuse toute requête sans `Origin` | Le navigateur envoie parfois `https://foo.com` et parfois `https://foo.com/` selon le contexte (POST XHR vs form) ; imposer un match strict conduirait à des faux refus dépendant du canal d'appel. L'absence d'`Origin` (curl anonyme, script serveur) est refusée car incompatible avec le cas d'usage réel (formulaire navigateur) | Ajouter une origine dans `CONTACT_ORIGIN_ALLOWLIST` (variable, séparateur virgule) sans se soucier du slash final. Refuser explicitement `null` origin (attaque connue) |
| 2026-08-02 | DEC-047 | `TurnstileVerifierFactory` échoue au boot si `TURNSTILE_ENABLED=true` sans `TURNSTILE_SECRET` (fail-closed), plutôt qu'un fallback silencieux vers l'implémentation permissive | Un site qui croit avoir un anti-bot actif mais qui accepte tout est plus dangereux qu'un site clairement sans anti-bot : les alertes sont fausses, les logs mensongers, et la porte est ouverte. Un `RuntimeException` au boot est bruyant, immédiat et vu par le déploiement | En dev/test : `TURNSTILE_ENABLED=false` par défaut → `AlwaysAllowTurnstileVerifier` explicite. En production : le secret est obligatoire, injecté par le gestionnaire de secrets, jamais dans Git |
| 2026-08-02 | DEC-048 | Générer un `Request-Id` UUID v7 pour chaque requête et l'exposer simultanément dans le corps JSON, l'en-tête `X-Request-Id` et les logs | UUID v7 est ordonné dans le temps → tri chronologique naturel des incidents sans champ `date` séparé. La triple exposition (body/header/log) permet à un visiteur qui reporte un incident de fournir un identifiant reproductible et au support de le retrouver instantanément dans les logs, sans exposer de PII | Toute réponse d'erreur de l'endpoint inclut `request_id` — utiliser cet identifiant comme clé de corrélation pour tout ticket support. Ne jamais réutiliser un `Request-Id` (généré à chaque requête, pas depuis le client) |
| 2026-08-02 | DEC-049 | `From` = adresse applicative Devzair (jamais l'email du visiteur), `Reply-To` = email du visiteur | Mettre l'email visiteur en `From` casse SPF/DKIM/DMARC et envoie l'email en spam. `Reply-To` permet une réponse naturelle en un clic depuis le client mail Devzair, sans compromettre l'authentification. Aucun HTML dans le corps (texte brut) élimine toute surface d'injection dans le rendu | La variable `CONTACT_FROM_EMAIL` doit rester sous domaine Devzair aligné SPF/DKIM. Ne jamais construire un en-tête email avec une entrée utilisateur brute (règle §14.9 du référentiel) |
| 2026-08-02 | DEC-050 | Caddy est le seul service à publier un port sur l'hôte ; `web` et `api` restent en `expose:` interne dans compose.yaml | Réduire la surface d'attaque en développement (aucun daemon Nuxt/Nitro ou PHP directement accessible depuis la LAN de dev), reproduire fidèlement l'architecture de production (un seul frontal), et permettre à Caddy d'ajouter TLS/HSTS/rate-limit sans dupliquer la config par service | `NUXT_PUBLIC_SITE_URL=http://localhost:3001` (Caddy). Aucun test ne doit taper directement `http://localhost:3000` ou `http://localhost:8000`. En prod : ajouter `tls internal` ou domaine automatique + `strict_transport_security`, et retirer `admin off` uniquement si l'admin socket est sécurisée par ailleurs |
| 2026-08-02 | DEC-051 | Pour toute interaction Playwright qui déclenche un handler Vue attaché au client (ex : ouverture du menu mobile), utiliser un pattern de clic retentable fondé sur l'état contractuel du DOM (`aria-expanded`), pas sur une sonde d'`__vue_app__` ni sur un délai fixe | En mode `nuxt dev` (Vite), `document.getElementById('__nuxt').__vue_app__` est posé par `mount()` avant que Vue n'ait effectivement attaché ses listeners `@click` sur le DOM initial. Un premier clic Playwright peut être dispatché *sans* handler : le DOM click event est bien émis, aucun listener ne le reçoit, l'état ne bascule pas, et le test échoue silencieusement sur l'assertion suivante (`expect(dialog).toBeVisible()` timeout). L'introspection de `__vue_app__` est nécessaire mais non suffisante. Un `waitForTimeout` masquerait la race sans la caractériser | Helper unique `openMobileNavigation(page)` dans `test/e2e/support/mobile-nav.ts` : `expect(async () => { await button.click(); await expect(button).toHaveAttribute('aria-expanded', 'true', { timeout: 500 }) }).toPass({ timeout: 10_000, intervals: [100, 200, 500] })`. Chaque nouvelle interaction sensible à l'hydratation (ex : soumission de formulaire côté client) doit adopter le même pattern : action → assertion sur un état observable contractuel → `toPass` avec intervals. Ne pas augmenter les retries Playwright ni les timeouts globaux pour masquer une race |
| 2026-08-02 | DEC-052 | Le composable `useContactForm` reste pur : il ne dépend pas des helpers Nuxt (`useFetch`, `useRuntimeConfig`, `$fetch`), l'endpoint et le fetcher sont injectés en argument | Coupler un composable au runtime Nuxt le couple mécaniquement à Vitest via un jeu de mocks (`vi.mock('#app')`, stubs de `useFetch`), ce qui masque les vraies branches de mapping HTTP (200/202/400/403/413/429 → codes ADR-007) derrière une machinerie de test. Injecter `endpoint` et `fetcher?` permet de tester les 20 cas unitaires avec un simple `globalThis.fetch` stubé, sans jamais démarrer un contexte Nuxt de test | Le seul point d'intégration Nuxt (`useRuntimeConfig().public.apiBaseUrl`) reste dans `ContactForm.vue` (le composant qui appelle le composable). Tout nouveau composable de domaine (contact, blog, ressources…) doit suivre le même contrat : effets injectés, pas d'accès direct au runtime |
| 2026-08-02 | DEC-053 | `TurnstileWidget.vue` bascule en mode « dev-noop » lorsque `NUXT_PUBLIC_TURNSTILE_SITE_KEY` est vide : il émet immédiatement `success('dev-noop')` et affiche un bandeau visuel « Mode dev » à côté du champ | Miroir côté navigateur de `AlwaysAllowTurnstileVerifier` (DEC-047) : sans site-key, en dev / test / preprod, le happy path ne doit pas être bloqué par une intégration Cloudflare réelle. Le bandeau visible écarte tout risque de confusion en production (un opérateur qui verrait ce bandeau en prod saurait immédiatement qu'il manque une variable). Le token `dev-noop` n'est jamais accepté par le vrai `CloudflareTurnstileVerifier` (réponse Cloudflare `invalid_input_response`) → aucun risque de bypass en production même si le widget est mal configuré | En production, `NUXT_PUBLIC_TURNSTILE_SITE_KEY` doit être renseigné et `TURNSTILE_ENABLED=true` côté API (fail-closed cf. DEC-047). Ces deux variables doivent être vérifiées ensemble en recette de mise en production. Ne jamais retirer le bandeau visuel « Mode dev » : c'est le seul signal opérateur de la bascule en fallback |
| 2026-08-02 | DEC-054 | Le honeypot serveur est silencieux (réponse 202 générique, aucun email envoyé) et l'UI affiche exactement le même bandeau succès qu'un envoi réel — jamais de message d'erreur spécifique au honeypot | Si l'UI affichait un message différencié quand le honeypot est rempli (« Requête rejetée », « Bot détecté », voire un simple statut d'erreur), un bot suffisamment sophistiqué détecterait ce contrat par différenciation de réponse et adapterait sa stratégie. La discrétion visuelle est le point d'intérêt du honeypot : le bot croit avoir réussi, ne réessaie pas, et son message ne part jamais | Le test E2E `honeypot rempli + réponse 200 → succès (contrat inchangé côté UI)` verrouille ce contrat. Ne jamais ajouter de log visible côté client (`console.warn`, message d'erreur) sur détection honeypot. Côté serveur, le canal Monolog `contact` peut noter l'événement sans PII (cf. DEC-045) pour ajuster la stratégie anti-abus |
| 2026-08-02 | DEC-055 | `MAILER_DSN` est la **seule** variable d'entrée du transport SMTP. Aucun nom d'hôte, aucun mot de passe, aucun port n'est présent en Git ou codé en dur dans `services.yaml`. Formes admises : `null://null` (dev/test), `smtps://user:pass@host:465` (TLS implicite, prod recommandée), `smtp://user:pass@host:587` (STARTTLS, hors prod) | Le domaine d'hébergement peut migrer (OVHcloud mutualisé → Exchange Pro → provider transactionnel comme SendGrid ou Postmark) sans redeploy applicatif ni PR sensible. La substitution en prod se fait via secret manager. Un nom d'hôte codé en dur casse à chaque migration ; le placer dans un `.env` versionné fait apparaître des détails d'infra dans Git | Toute exception `TransportExceptionInterface` doit remonter en `ContactTemporarilyUnavailableException` (cf. DEC-057). `bin/console app:contact:check` refuse `null://null` en prod (erreur bloquante) et refuse `smtp://` plaintext en prod (erreur bloquante). Le secret vit dans le secret manager ; ne jamais le logguer, ne jamais l'exposer dans la sortie CLI |
| 2026-08-02 | DEC-056 | Turnstile est facultatif via **deux flags alignés** : `TURNSTILE_ENABLED` (API, défaut `false`) et `NUXT_PUBLIC_TURNSTILE_ENABLED` (front, défaut `false`). Divergence = rejet systématique côté API ou script Cloudflare chargé pour rien | Un flag serveur seul ne suffit pas : sans flag front, le navigateur continuerait à charger `challenges.cloudflare.com/turnstile/v0/api.js`, à monter l'iframe et à collecter (au minimum) l'IP et l'user-agent du visiteur — dépendance tierce et question RGPD pour un formulaire potentiellement peu soumis. Deux flags rendent l'état visible et symétrique : opérateur voit immédiatement si l'un est mal réglé. Le défaut `false` est sûr (aucun script, `dev-noop` accepté par `AlwaysAllowTurnstileVerifier`) | Checklist prod (`docs/checklists/PRODUCTION-CONTACT.md` §2) vérifie l'alignement en premier. Test E2E « Turnstile désactivé (défaut) : aucun script Cloudflare chargé » verrouille l'absence de requêtes vers `challenges.cloudflare.com` quand `NUXT_PUBLIC_TURNSTILE_ENABLED=false`. Une bannière visuelle « Mode dev — protection anti-bot désactivée » du `TurnstileWidget` reste affichée dans ce mode : c'est le seul signal opérateur d'un fallback en production, ne jamais la retirer |
| 2026-08-02 | DEC-058 | Livrer d'abord les deux pages institutionnelles `/agence` et `/expertises` (Phase 7A), et **ne pas** créer les 5 pages `/expertises/{slug}` en même temps (Phase 7B) | Les 5 pages détaillées demandent un brief éditorial validé par pôle (positionnement, livrables, garanties, appels à l'action) que le client n'a pas encore signé — les publier au même moment qu'une architecture éditoriale non éprouvée risquerait de figer prématurément une navigation profonde à corriger ensuite. Livrer 7A seule permet de valider la maille éditoriale (composants, tokens, tone) et l'ergonomie inter-pages sur 3 pages publiques avant de démultiplier le contenu. La config typée `expertise-pages.ts` (`status: "planned"`) déclare l'intention sans introduire de route morte | Aucune route `/expertises/{slug}` ne doit exister tant que Phase 7B n'est pas lancée. Les tests unitaires (`config/expertise-pages.spec.ts`) et E2E (`institutional-pages.spec.ts`) verrouillent l'absence de tout `<a href^="/expertises/">` en 7A. Basculer `status: "planned"` → `status: "live"` uniquement à la publication réelle d'une page (une entrée à la fois), pas au moment où le contenu est simplement rédigé |
| 2026-08-02 | DEC-059 | Ajouter un champ `isRoute: boolean` sur chaque `NavigationItem` (et sur `primaryCta`), et l'utiliser côté `SiteHeader`/`SiteFooter`/`MobileNavigation` pour piloter `NuxtLink :external` : `isRoute: true` → route Nuxt interne (`external: undefined`), `isRoute: false` → ancre ou lien externe (`external: true`) | La règle héritée du bootstrap (« hard-coded `external` sur toutes les entrées de nav ») évitait les warnings `[VUE_ROUTER_R0004] No match found` mais empêchait `NuxtLink` d'exploiter la préfetch et le SPA-routing sur les entrées vraiment internes. Une fois `/agence` et `/expertises` livrées, garder `external` figé aurait signifié une navigation full-reload sur toutes les pages du site, alors que le seul cas légitime de `external` est l'ancre same-page (`/#realisations`, `/#contact`) où vue-router ne peut pas résoudre. Le champ typé rend le contrat explicite dans la config, testable, et refactorisable en un endroit | Ajouter une entrée dans `primaryNavigation` / `footerNavigation` sans `isRoute` produit une erreur TypeScript. Une nouvelle route interne doit passer par `isRoute: true` ; une ancre / lien externe par `isRoute: false`. Les composants layout ne contiennent aucune logique de détection (ex. `to.startsWith('#')`) — la source de vérité reste la config |
| 2026-08-02 | DEC-060 | `ExpertiseOverviewCard.vue` publie un `<article>` avec un H3 **non lié** (aucun `<a>`, aucun `tabindex`, aucun `role="button"`) tant que Phase 7B n'est pas livrée | La carte est présentée en 7A comme une **vitrine informative** des cinq pôles, pas comme un point d'entrée navigation. Livrer une carte cliquable en 7A obligerait à choisir entre (a) pointer vers une route inexistante (`/expertises/concevoir`) — dead link, régression accessible, warning vue-router R0004 — ou (b) désactiver visuellement le lien via `aria-disabled` ou `tabindex="-1"` — anti-pattern accessible qui trompe les technologies d'assistance. Publier une carte non interactive respecte le contenu réel (le pôle est décrit, pas exploré en profondeur) et laisse le composant refactorable en une seule ligne quand Phase 7B fournira le slug | Passage à Phase 7B : le H3 devient `<NuxtLink :to="pillarPage.route">{{ pillar.label }}</NuxtLink>`. Aucun changement de sémantique (`<article>` + H3 restent), aucun changement de tests structurels (`.expertise-overview-card`, badge d'ordre, description, services). Le test unitaire `ExpertiseOverviewCard.spec.ts` « never renders a link » doit être supprimé en 7B et remplacé par un test miroir « renders exactly one link toward pillar page ». Les tests E2E `institutional-pages.spec.ts` « ne rend aucune carte comme lien tant que Phase 7B n'est pas livrée » doivent être migrés dans la même PR |
| 2026-08-02 | DEC-061 | `EditorialSection.vue` génère son `id` d'`aria-labelledby` via `useId()` (pas un id littéral, pas un compteur module) | Une page (`/expertises`) peut monter plusieurs `EditorialSection` (`approche` + `nos cinq pôles`). Un id littéral (`editorial-section-title`) provoquerait deux DOM ids identiques → violation WCAG 4.1.1, `aria-labelledby` ne pointant plus de manière univoque. Un compteur module (`let counter = 0`) casserait la SSR hydration (l'ordre de mount côté serveur vs client peut diverger si la page utilise `<ClientOnly>`). `useId()` Vue 3.5+ produit un id stable SSR ↔ client, unique par instance, sans state global | Tout composant éditorial qui expose un `aria-labelledby` ou `aria-describedby` interne doit utiliser `useId()` — `EditorialHero`, `EditorialCallout`, `TurnstileWidget` (existant) suivent le même pattern. Ne pas remplacer par un id explicite reçu en prop, sauf si l'appelant a besoin d'un ancre stable (dans ce cas la prop `sectionId` alimente `<section :id>`, pas l'`aria-labelledby`) |
| 2026-08-02 | DEC-058 | Séparer `expertise-pages.ts` (routing / statut par slug) de `expertise-pillars.ts` (contenu narratif partagé) plutôt que fusionner en une seule config | `expertise-pillars.ts` sert simultanément le SVG hero, la grille détaillée `HomeExpertisePillars`, `HomeConnectedApproach` (parcours 5 étapes) et désormais la grille `/expertises` — ajouter des champs de routage (`slug`, `route`, `title`, `status: "planned"\|"live"`) à ce contrat le polluerait pour tous les consommateurs qui n'en ont rien à faire. Deux configs distinctes, chacune source unique de vérité sur son axe, référencées entre elles par `pillarId` | Toute page 7B doit lire son entrée dans `expertise-pages` (routage + statut) et la fusionner avec le pillar correspondant (`expertisePillars.find(p => p.id === pillarId)`) pour le narratif. Basculer `status: "planned"` → `"live"` **uniquement** quand la route existe réellement dans `app/pages/expertises/`. Tests unitaires `expertise-pages.spec.ts` garde-fous : slugs uniques, ids uniques, mapping bijectif vers `expertisePillars`, services miroir |
| 2026-08-02 | DEC-059 | Champ `isRoute: boolean` sur `NavigationItem` pilotant `NuxtLink :external` (`:external="item.isRoute ? undefined : true"`), plutôt qu'un `external` codé en dur ou une heuristique à base de préfixe `#` | La Phase 7A introduit un mélange de vraies routes (`/agence`, `/expertises`) et d'ancres same-page (`/#realisations`, `/#contact`). Coder `external` en dur casse les vraies routes internes (perte du prefetch et de la SPA nav) ; une heuristique regex sur `to.startsWith('/#')` marche pour l'accueil mais faux positive dès qu'on veut cibler `/agence#valeurs`. Un flag explicite dans la config est lisible, testable, aligné avec le contrat (« ceci pointe-t-il vers une route Vue Router existante »). Il remplace DEC-017 (prop `external` sur `BaseButton`/`BaseLink`) pour le domaine navigation | À chaque nouvelle route livrée (7B, 7C…), passer `isRoute: true` sur l'entrée correspondante de `primaryNavigation` / `footerNavigation` — c'est la seule modification requise pour que la nav bascule en SPA nav. Les entrées ancres restent `isRoute: false`. Vérifier au review : jamais de `isRoute: true` pour une route qui n'existe pas encore dans `app/pages/` |
| 2026-08-02 | DEC-060 | En Phase 7A, `ExpertiseOverviewCard` ne rend **aucun** `<a>` : H3 texte pur, badge d'ordre `aria-hidden`, aucun `tabindex`, aucun bouton | Les 5 routes `/expertises/{slug}` n'existent pas encore (Phase 7B). Rendre les cartes comme liens produirait 5 liens morts et casserait `no dead links` + DEC-017. Retirer l'interactivité entièrement (plutôt que `href="#"` ou un lien désactivé) évite tout signal ambigu à l'utilisateur clavier / lecteur d'écran : la carte est un article informatif, point final. La bascule 7A → 7B se fait en une seule ligne (wrap du H3 dans `<NuxtLink>`) le jour où les routes existent | Test unitaire `ExpertiseOverviewCard.spec.ts::"does not carry any focusable element"` verrouille l'absence de `a, button, [tabindex]`. Test E2E `institutional-pages.spec.ts::"ne rend aucune carte comme lien tant que Phase 7B n'est pas livrée"` verrouille `.expertise-overview-card a` count === 0 **et** `a[href^="/expertises/"]` count === 0. Retirer ces deux tests uniquement à la publication effective de 7B |
| 2026-08-03 | DEC-058 | Introduire deux configs typées disjointes pour les pôles d'expertise : `expertise-pillars.ts` (contenu éditorial partagé) et `expertise-pages.ts` (routing/status/services de la page dédiée) | Un pôle (`expertisePillars[i]`) n'est pas une page (`expertisePages[i]`) — le premier est consommé par le SVG hero, la grille d'accueil et la vue d'ensemble `/expertises`, le second par la Phase 7B (routes `/expertises/{slug}`). Fusionner casserait le contrat existant et forcerait un `status` sur toutes les vues. `expertise-pages.ts` référence `expertise-pillars.ts` par `pillarId` (test unitaire garde-fou : `id === pillarId`, `services` miroir des `pillar.services` — divergence détectée immédiatement) | Toute nouvelle donnée éditoriale de pôle (long description, tagline, résultats mesurables) va dans `expertise-pillars.ts`. Toute nouvelle donnée de la page `/expertises/{slug}` (contenu long, blocs additionnels, CTA propres) va dans `expertise-pages.ts`. Le basculement `status: "planned"` → `status: "live"` en Phase 7B ne se fait qu'à la publication réelle de la page (pas au premier commit de contenu) |
| 2026-08-03 | DEC-059 | Les cartes `ExpertiseOverviewCard` ne sont **pas** cliquables en Phase 7A (aucun `<a>`, aucun `tabindex`, aucun `role="button"`) | Aucune route `/expertises/{slug}` n'existe en 7A. Rendre le H3 en `NuxtLink` sur une route absente déclencherait `[VUE_ROUTER_R0004]` (cf. DEC-017) et signalerait au visiteur qu'il peut naviguer là où il ne peut pas. Le brief 7A demande une vue d'ensemble éditoriale, pas un catalogue navigable. Le badge d'ordre `01`…`05` reste `aria-hidden="true"` (décoratif — la source de vérité est `pillar.order`) | Tests garde-fous : unitaire `ExpertiseOverviewCard.spec.ts` (« does not carry any focusable element ») + E2E `institutional-pages.spec.ts` (« ne rend aucune carte comme lien tant que Phase 7B n'est pas livrée ». En Phase 7B, transformer le H3 (pas la carte entière) en `NuxtLink` — cible clic large mais unique cible focusable, conforme à WCAG 2.4.4 |
| 2026-08-03 | DEC-060 | Ajouter un champ `isRoute: boolean` sur `NavigationItem` pour piloter `NuxtLink :external` par item, plutôt qu'un `external` hardcodé au niveau du composant layout | Après Phase 7A la nav mélange 3 vraies routes (`/`, `/agence`, `/expertises`) et 1 ancre same-page (`/#realisations`) et 1 ancre CTA (`/#contact`). Un `:external="true"` uniforme fait perdre le comportement client-side sur les routes réelles ; un `:external="false"` uniforme redéclenche les warnings vue-router R0004 sur les ancres. `isRoute` est déclaratif dans la source unique `navigation.ts` — le composant applique `:external="item.isRoute ? undefined : true"` | Chaque nouvelle entrée de nav documente explicitement `isRoute: true` (route Nuxt existante) ou `false` (ancre same-page, mailto, external). Retirer une route → passer `isRoute` à `false` ou retirer l'entrée. Trois composants layout partagent la règle : `SiteHeader.vue`, `SiteFooter.vue`, `MobileNavigation.vue` |
| 2026-08-03 | DEC-061 | `EditorialSection.vue` utilise `useId()` par instance pour son `aria-labelledby`, plutôt qu'un id dérivé du `title` ou un id manuel via prop | Deux sections avec le même titre (rare mais possible : « Notre approche » sur `/agence` et sur `/expertises` du même onglet) doivent avoir des ids distincts pour ne pas casser l'association aria. Dériver du titre demande de slugifier (charge inutile) et n'élimine pas la collision. `useId()` est SSR-safe (Vue 3.5+), stable entre serveur et client, et unique par instance sans configuration | Toute nouvelle section éditoriale suit le même pattern (`aria-labelledby` sur `<section>`, `id` sur le H2 via `useId()`). Une prop `sectionId` reste possible pour ancrer une section dans une URL (`/agence#nos-valeurs`) sans casser l'aria (l'`id` visible et l'`aria-labelledby` sont deux attributs distincts sur deux nœuds distincts) |
| 2026-08-02 | DEC-057 | Un échec SMTP se traduit par **HTTP 503 `temporary_error`**, jamais par un 200/202 « acquitté ». Le contrat front : bandeau verbatim « Service momentanément indisponible » / « Le service est momentanément indisponible. Votre message n'a pas été envoyé. Merci de réessayer plus tard. », affichage du `request_id`, **valeurs saisies conservées**, aucun retry navigateur automatique | Un 202 muet trompe l'utilisateur (il croit son envoi acté alors que le message est perdu) et masque l'incident aux ops. Un 500 générique ne distingue pas panne transitoire vs bug applicatif et provoque des tickets support inutiles. Un 503 explicite avec code métier stable permet au front d'afficher un message actionnable, à l'utilisateur de retenter manuellement avec ses valeurs, et aux ops de tracer l'incident via le log `contact.mailer_unavailable` (canal `contact`, warning, aucun PII — cf. DEC-045). `CONTACT_RECIPIENT` absent en prod est traité identiquement : `ContactTemporarilyUnavailableException` levée avant même l'envoi | Toute nouvelle branche d'erreur transport / configuration bloquante à l'envoi doit lever `ContactTemporarilyUnavailableException` plutôt qu'un `RuntimeException` générique — c'est le marker que le contrôleur mappe sur 503. Tests garde-fous : `SymfonyContactMessageSenderTest::testSendConvertsTransportException…`, `ContactSubmissionControllerTest::testMailerFailureReturns503…` (vérifie que le payload reste dans le champ `errors` côté front), `ContactLoggingTest::testMailerFailureLogsWarningWithoutPii`. Le test Playwright 503 verrouille le bandeau verbatim et les valeurs préservées côté navigateur |
| 2026-08-03 | DEC-062 | En Phase 7B, la bascule « carte informative » → « carte cliquable » sur `ExpertiseOverviewCard.vue` se fait via `<NuxtLink v-if="isPublished()">` + `<article v-else>` (deux blocs distincts avec contenu dupliqué), et **jamais** via `<component :is="isPublished() ? 'NuxtLink' : 'article'">` | La forme `<component :is="'NuxtLink'">` (chaîne littérale) laisse dans le DOM un `<nuxtlink>` custom element non résolu à l'hydratation : le composant NuxtLink n'est pas enregistré comme HTML tag global, seulement comme composant Vue à résoudre au compile-time. Le rendu SSR produit `<nuxtlink to="…">…</nuxtlink>` et le navigateur le traite comme un élément inconnu → pas de navigation SPA, warnings d'hydratation, lien non cliquable au JS. La forme `v-if`/`v-else` avec deux templates duplique quelques lignes mais garantit que le compilateur Vue résout `NuxtLink` en composant réel dans les deux branches | La duplication de contenu (badge d'ordre, titre, description, services) est acceptée comme coût nécessaire à l'hydratation correcte. Toute future carte conditionnellement cliquable (Phase 7C études de cas, Phase 9 articles blog) doit suivre le même pattern — jamais `<component :is>` sur un composant Nuxt/Vue déclaré (par opposition à un tag HTML natif comme `h1`/`h2`, où `:is` fonctionne). Tests garde-fous : `ExpertiseOverviewCard.spec.ts` vérifie `wrapper.element.tagName === 'A'` (published) vs `=== 'ARTICLE'` (planned) |
| 2026-08-03 | DEC-063 | Les CTA `Découvrir ce pôle →` sur `ExpertiseOverviewCard.vue` et `ExpertiseRelatedPillars.vue` utilisent `--color-petrol` (avec `font-weight: 700`), **pas** `--color-devzair-blue` — malgré l'appartenance de ce dernier à la palette « accent primaire » | Sur les fonds cream (`#efebe2`) et sand (utilisés par les cartes overview et related), `--color-devzair-blue` (`#2e86d9`) donne un ratio de contraste de **3.19:1** — sous le seuil WCAG 2.2 AA de 4.5:1 pour le petit texte normal, même en `font-weight: 700` (le seuil « large text » 3:1 ne s'applique qu'aux 18pt+ ou 14pt+ bold, ce qui n'est pas le cas de 0.875rem/700). `--color-petrol` (`#0c5b57`) donne un ratio de ~6.05:1 sur cream et ~6.4:1 sur sand — largement au-dessus du seuil. Axe (`color-contrast`) verrouille cette règle dans `expertise-pages.spec.ts` | Toute nouvelle chaîne CTA texte sur fond clair (cream/sand/background-primary) doit préférer `--color-petrol` à `--color-devzair-blue`. La règle inverse s'applique sur fond navy foncé (`--color-devzair-blue` reste utilisable en accent sur navy, ratio ≥ 7:1). Ne pas ajouter un `background-color: transparent` autour d'un texte devzair-blue pour « corriger » — le fond de la carte est ce qui compte, pas le fond du texte. Vérifier tout nouveau CTA texte via Axe avant merge |
| 2026-08-03 | DEC-064 | La route dynamique `/expertises/[slug].vue` lance `createError({ statusCode: 404, fatal: true })` pour tout slug inconnu ou en statut non-`published`, **jamais** de fallback silencieux (redirection vers `/expertises`, rendu d'une page vide, `notFound: true`) | Un fallback silencieux (redirection ou page vide) masque au moteur de recherche que la ressource n'existe pas → l'URL est indexée avec un contenu générique, dilue le PageRank, et fragilise le fil d'Ariane utilisateur (il croit être sur la page X, il est sur la page racine). Un 404 explicite est le comportement RFC 7231 attendu : `error.vue` prend le relais avec un message clair, le crawler enregistre l'URL comme non-ressource et l'écarte de l'index. `fatal: true` interrompt le rendu immédiatement (pas de tentative de mount de la page derrière l'erreur) | Toute route dynamique future (Phase 9 `/ressources/[slug]`, Phase 7C `/realisations/[slug]`) doit suivre le même pattern. Le test Vitest `expertise-slug.spec.ts::"lance createError({statusCode:404, fatal:true}) pour un slug inconnu"` et le test Playwright `expertise-pages.spec.ts::"HTTP 404 sur un slug inconnu"` verrouillent le contrat serveur+client. Ne jamais ajouter de `catch` autour du `find` — laisser l'exception propager |
| 2026-08-03 | DEC-065 | Le JSON-LD Schema.org `Service` émis par `useExpertiseServiceSchema` référence le `provider` par son `@id` (`${origin}/#organization`), **pas** en dupliquant les champs de l'Organization | Google et les autres moteurs de connaissance (schema.org validators) préfèrent une entité `Organization` unique par domaine, référencée par `@id` depuis les autres nœuds (`Service`, `Article`, `Product`). Dupliquer les champs `name`, `url`, `logo` sur chaque `Service` (a) alourdit le HTML de ~500 octets par page, (b) crée un risque de divergence entre les copies au fil des mises à jour de la fiche entreprise, (c) peut être interprété comme plusieurs organizations distinctes par les moteurs stricts. L'`@id` référence l'Organization du layout (`useSiteSchema`), qui reste la source unique | Tout futur type Schema.org emis par une page (Article Phase 9, Review, FAQPage) doit suivre le même pattern : `provider`/`author`/`publisher` référencé par `@id`, jamais dupliqué. Le format de l'`@id` est `${origin}/#organization` (fragment `#organization` stable, un seul par domaine). Test Vitest `useExpertiseServiceSchema.spec.ts` verrouille l'`@id` et le format `provider: { "@id": ... }` |
| 2026-08-03 | DEC-066 | La liste `relatedPillarIds` sur `ExpertisePageDefinition` est de longueur exacte **2** par entrée (verrouillé par test unitaire), et une entrée ne se référence jamais elle-même | Deux pôles connexes forment un signal éditorial équilibré (assez pour proposer un maillage utile, pas trop pour éviter la surcharge cognitive en fin de page). Un lien vers soi-même (`concevoir` → `concevoir`) serait à la fois un lien inutile pour l'utilisateur et un signal SEO circulaire. La règle est explicite dans le contrat typé (`readonly [string, string]` serait plus strict mais casse la lecture map — on préfère `readonly string[]` + assertion runtime dans les tests) | Toute nouvelle entrée dans `expertise-pages.ts` doit fournir exactement 2 ids valides et différents de son propre `pillarId`. Le test `config/expertise-pages.spec.ts` échoue immédiatement sinon. `ExpertiseRelatedPillars.vue` filtre en plus par `status === "published"` : une cible référencée mais non publiée est silencieusement ignorée à l'affichage — permet de mettre en attente une page sans casser les pages qui la référencent |

---

## 26. Journal des changements

| Date | Version | Changement |
|---|---|---|
| 2026-08-01 | 1.0 | Création du référentiel initial |
| 2026-08-01 | 2.0 | Architecture Nuxt/Docker/Symfony confirmée, phases réorganisées, SOLID/DRY et implémentation SEO/GEO Nuxt ajoutés |
| 2026-08-01 | 2.1 | Lot design system posé (Phase 3 partielle) : tokens CSS, composants base, layout (header/mobile nav/footer), composable `useMobileNavigation`, page interne `/design-preview` noindex, Vitest opérationnel (20 tests verts), typecheck et build verts |
| 2026-08-02 | 2.2 | Clôture des fondations techniques (Phases 1 et 2) : `.gitignore` racine, `Dockerfile.dev` reproductible (npm ci au build, plus de `npm install` à chaque boot), `compose.yaml` refactorisé, `.env.example` sans secret, ESLint Flat Config via `@nuxt/eslint`, Playwright installé avec smoke test `/design-preview`, `@nuxt/fonts` en charge des polices, workflow CI `web-quality.yml`, READMEs racine et `apps/web` mis à jour. `npm run quality` vert (lint, typecheck, tests 20/20, build) |
| 2026-08-02 | 2.3 | Clôture Phase 3 — Design system et accessibilité : suite E2E Playwright complète (6 fichiers, 28 tests) sur `/design-preview` (H1/robots/skip-link/header/footer/console, mobile-navigation avec focus-trap et scroll-lock, keyboard-navigation avec anneau focus, responsive 390/768/1440, reduced-motion, Axe WCAG 2.2 AA sans `disableRules`). Défauts démontrés corrigés (SiteFooter contraste `--color-cream-subtle`, swatch Devzair blue en noir pur). Job CI `e2e` dans l'image officielle Playwright, scripts `test:e2e:ui` et `quality:full`. Conventions de nommage documentées dans `docs/06-ARCHITECTURE-CODE.md` §16.9. `npm run quality:full` vert (lint, typecheck, tests unitaires 20/20, build, E2E 28/28) |
| 2026-08-02 | 2.5 | Phase 5A — Accueil : hero + graphe SVG des cinq pôles. `HomeHero.vue` (eyebrow, H1, introduction, deux CTA vers `#contact` et `#realisations`, réassurance à 2 items), `HomeEcosystemGraph.vue` (SVG accessible `role="img"` + `<title>`/`<desc>` liés par `aria-labelledby` via `useId()`, sous-groupes `aria-hidden`, animation signature CSS ~1,4 s non bouclée, défense en profondeur `prefers-reduced-motion`), `app/config/expertise-pillars.ts` (contrat typé, source unique pour les cinq pôles). `pages/index.vue` recâblée sur `usePageSeo({ title: "Agence digitale pour sites web…", type: "website" })`, canonical `/`. `routeRules['/'] = { prerender: true }` : deux routes prérendues au build (`/` + `/_payload.json`). Image OG statique reportée (aucun logo Devzair disponible — cf. DEV-026). Curl SSR : `<html lang="fr">`, `og:type=website`, `twitter:card=summary` (pas d'image), aucun `og:image` émis. Tests : 69 unitaires (+17) et 51 E2E (+13) verts, `npm run quality` OK. |
| 2026-08-02 | 2.7 | Phase 5C — Accueil : sections « Réalisations » (état honnête), « Notre méthode », « Pourquoi Devzair ». Deux configs typées créées (`app/config/project-process.ts` 6 étapes, `app/config/trust-promises.ts` 5 promesses, texte verbatim brief 5C). Trois composants : `HomeFeaturedCaseStudy.vue` (ancre `#realisations`, variante « en préparation » — aucun faux client, aucun logo, aucun bouton vers route absente ; cf. DEC-035), `HomeProcess.vue` (`<ol>` 6 étapes verbatim `Découverte`/`Cadrage`/`Conception`/`Développement`/`Lancement`/`Évolution`, timeline verticale mobile via pseudo-éléments, 2 col ≥640px, grille 3×2 ≥1024px — 6 col refusées pour la lisibilité, cf. DEC-037), `HomeTrust.vue` (`<ul>` 5 promesses verbatim, 1 col mobile, 2 col+row-span ≥640px, grille asymétrique 3+2 centrée ≥1024px). `pages/index.vue` orchestre 7 sections. `navigation.ts` : lien header « Réalisations » recablé de `/realisations` vers `/#realisations` (cf. DEC-036). CTA hero « Découvrir nos réalisations » opérationnel sans modification. Sémantique WCAG 2.2 AA respectée : un seul H1, un H2 par section, aucune interaction fantôme. Curl SSR : 18 signatures éditoriales exactes présentes une seule fois (H2, honest-state, 6 labels méthode, 5 labels promesses, `id="realisations"`, `/#realisations`). Tests : 120 unitaires (+30) et 97 E2E (+25 dans `home-sections-secondary.spec.ts` — ordre 7 sections, honest-state, CTA hero, header nav, Axe, reduced-motion, responsive 320/390/768/1024/1440) verts. Build : 2.83 MB / 733 kB gzip (+45 KB / +8 KB gzip vs 5B). Sections différées : FAQ éditoriale, CTA final, ancre `#contact` (Phase 5D/6). |
| 2026-08-02 | 2.6 | Phase 5B — Accueil : sections « Le constat », « La réponse Devzair », « Nos expertises · 5 pôles ». `expertise-pillars.ts` étendu (`longDescription`, `services: readonly string[]`, variante `accent`) — 100 % rétro-compatible avec `HomeEcosystemGraph.vue` qui ne teste que `variant === 'primary'`. Trois composants créés : `HomeProblems.vue` (constat, `<ol>` 5 items, numérotation Space Mono, 2 colonnes ≥768px), `HomeConnectedApproach.vue` (parcours 5 étapes sur fond navy, source unique `expertisePillars`, flèches ↓/→ décoratives `aria-hidden`, `longDescription` en `sr-only` pour AT), `HomeExpertisePillars.vue` (ancre `#expertises`, carrousel scroll-snap mobile CSS-natif sans JS ni boutons, 2 col tablet, grille asymétrique 3×2 desktop avec Concevoir spans 2 rows et Faire évoluer petrol). `pages/index.vue` orchestre les 4 sections. Corrections Axe : `HomeConnectedApproach.step-index` passé sur cream (Devzair-blue échouait à 4.41:1 sur navy-elevated), carte accent passée sur cream/cream-muted (Devzair-blue échouait à 2.08:1 sur petrol) — cf. DEV-028 / DEC-034. Curl SSR : 3 H2 présents, 5 items par section, `id="expertises"` et « Faites défiler » présents. Tests : 90 unitaires (+21) et 72 E2E (+21) verts. Build : 2.79 MB / 725 kB gzip (+40 KB / +11 KB gzip). Sections différées documentées : réalisations Phase 5C/7, méthode / pourquoi / CTA final Phase 5C, contact Phase 6. |
| 2026-08-02 | 2.4 | Clôture Phase 4 — Socle SEO technique. Modules `@nuxtjs/robots` v5.7 et `@nuxtjs/sitemap` v7.6 (Option 2 « à la carte », cf. ADR-004). Utilitaires purs `site-url.ts` et `canonical.ts`. Composables `usePageSeo` (canonical + OG + Twitter + robots) et `useSiteSchema` (JSON-LD Organization + WebSite, injecté une fois par le layout). `runtimeConfig.public` étendu à `siteUrl` et `siteIndexable` (défaut `false` — safe par défaut, une seule source de vérité). Pages `/` et `/design-preview` migrées vers `usePageSeo({ noindex: true })`. Tests unitaires étendus (52/52) : `site-url.spec.ts`, `canonical.spec.ts`, `usePageSeo.spec.ts`. Tests E2E étendus (38/38) : `seo.spec.ts` couvre HTML SSR, X-Robots-Tag, robots.txt et sitemap.xml. Curl confirme : `<html lang="fr">`, titre `Devzair — Chantier en cours \| Devzair`, `meta robots noindex, nofollow`, JSON-LD Organization + WebSite, `og:*` absolus, aucune canonical sur pages noindex, `X-Robots-Tag: noindex, nofollow`, `robots.txt : User-agent: * / Disallow: /`, sitemap XML absolu sans `/design-preview`. Aucune donnée fictive (contact, sameAs, aggregateRating). Image OG par défaut différée (`public/og/.gitkeep`, `defaultOgImage = null`). `npm run quality` vert (lint, typecheck, tests 52/52, build). |
| 2026-08-02 | 2.9 | Phase 6A — Endpoint `POST /api/contact` (backend only, sans UI). Scaffold `apps/api` Symfony 7.4 LTS / PHP 8.4 sans DB, sans Doctrine, sans admin (cf. ADR-006). Caddy 2 devient l'unique service exposé sur l'hôte (`3001:80`), route `/api/**` vers `api:8000` avec `uri strip_prefix /api`, tout le reste vers `web:3000` (cf. DEC-050). Pipeline endpoint (cf. ADR-007) : Origin allowlist (`OriginAllowlist` normalise le trailing slash — DEC-046, 403 `origin_not_allowed`) → payload > 10 KB (413) → rate limiter token bucket par IP réelle (`limiter.contact_ip`, 429 + `Retry-After`) → JSON parse (400 `invalid_json`) → honeypot `website` silencieux (202 générique sans email) → validation stricte `ContactRequest` DTO (Symfony Validator : email, longueurs, regex téléphone, Choice `projectType`, IsTrue `consent`) → Turnstile serveur (`CloudflareTurnstileVerifier` + `TurnstileVerifierFactory` fail-closed au boot si secret manquant — DEC-047) → `SubmitContactMessage` → `SymfonyContactMessageSender` (`From` app / `Reply-To` visiteur / texte brut / `X-Request-Id` — DEC-049). `MAILER_DSN=null://null` par défaut (DEC-044). `Request-Id` UUID v7 dans le corps JSON + `X-Request-Id` + logs (DEC-048). Canal Monolog `contact`, aucun PII loggué (DEC-045), test `ContactLoggingTest` garde-fou. CSRF : Option B stateless, aucune session, aucun cookie (DEC-043). CI : nouveau workflow `.github/workflows/api-quality.yml` (PHP 8.4, `composer validate --strict`, `lint:yaml config`, `lint:container --env=test`, PHPUnit) déclenché sur `apps/api/**`. Suite PHPUnit 11.4 : `ContactRequestValidationTest` (data provider 6 cas invalides), `OriginAllowlistTest`, `TurnstileVerifierFactoryTest` (fail-closed), `CloudflareTurnstileVerifierTest` (MockHttpClient), `HealthControllerTest`, `ContactSubmissionControllerTest` (7 cas dont honeypot 202, validation 400, payload 413, headers), `ContactRateLimitTest` (429 + Retry-After), `SymfonyContactMessageSenderTest` (From app / Reply-To visiteur / X-Request-Id / null recipient throws), `ContactLoggingTest` (aucune PII sur happy path). Playwright stabilisé (DEV-038) : 5 `page.waitForLoadState('networkidle')` supprimés dans `mobile-navigation.spec.ts`, `keyboard-navigation.spec.ts` (2×), `reduced-motion.spec.ts`, `home-cta-final.spec.ts` — remplacés par `goto('/', { waitUntil: 'domcontentloaded' })` + `locator(<élément hydraté>).waitFor({ state: 'visible' })`. CI Playwright conserve `workers: 1`, `retries: 1`, reporter enrichi `[['line'], ['github'], ['html', ...]]` pour distinguer passed/flaky/failed en stdout. Nouveaux ADRs : ADR-006 (runtime PHP built-in server + Caddy, ADR-008 différé PostgreSQL/Doctrine), ADR-007 (CSRF Option B, 6 filtres, table des réponses HTTP, table logging). Docs mises à jour : `README.md` racine (architecture 3 services, smoke curl), `apps/api/README.md` créé (stack, pipeline, dev local, variables, non-goals explicites), `docs/05-SECURITY-PRIVACY.md` §14.9 + §14.11, `docs/06-ARCHITECTURE-CODE.md` §16.1/§16.2/§16.10 (ADR renumérotés), `docs/07-QUALITY-DELIVERY.md` §20 (jobs CI actifs + note Playwright). Non-goals confirmés Phase 6A : aucun formulaire Nuxt, aucun widget Turnstile navigateur, aucune page `/contact`, aucune PostgreSQL, aucun admin, aucun stockage de message, aucun CRM, aucune queue asynchrone, aucune image OG — livrés en Phase 6B ou plus tard. |
| 2026-08-02 | 2.10 | Correctif Playwright — élimination du test flaky `home-cta-final.spec.ts:128` (« mobile menu opens, activates the CTA, closes, and lands on #contact ») et stabilisation transverse des interactions clavier/menu mobile. **Cause racine** : en `nuxt dev`, `document.getElementById('__nuxt').__vue_app__` est posé par `mount()` avant que Vue n'ait attaché ses listeners `@click` sur le DOM initial ; le premier clic Playwright peut être dispatché sans handler → l'état `isOpen` ne bascule pas, `#mobile-navigation` n'est jamais rendu, et le test échoue en aval sur `expect(dialog).toBeVisible()`. L'ancien helper `waitForHydration` (introspection de `__vue_app__`) est nécessaire mais insuffisant. **Correction** (cf. DEC-051) : nouveau helper `test/e2e/support/mobile-nav.ts::openMobileNavigation(page)` qui retente `button.click()` tant que `aria-expanded` n'a pas basculé à `'true'`, via `expect(...).toPass({ intervals: [100, 200, 500] })`. Ancien helper `test/e2e/support/hydration.ts` supprimé (code mort). Tests migrés : `home-cta-final.spec.ts` (test CTA + capture erreurs console navigation/hydratation), `mobile-navigation.spec.ts` (3 tests : opens+focus, closes on link, tab-trap), `reduced-motion.spec.ts` (mobile nav ouverture). Assertions renforcées sur le test CTA : ajout `aria-expanded` true/false explicites et check absence d'erreurs console `/navigat|hydrat|router|teleport|vue|nuxt/i`. **Vérification** : test ciblé répété 20× sans retry → 20 passed. Suite complète 109 tests `--retries=0` → 109 passed. Suite complète en config CI habituelle (`workers=1 retries=1`) → 109 passed, 0 flaky, 0 failed. `npm run lint` + `npm run typecheck` + `npm run test` (130/130) + `npm run build` (2.8 MB / 728 kB gzip) + `php bin/phpunit` (31/31, 68 assertions) tous verts. Aucun `waitForTimeout` ajouté, aucun `force: true`, aucun timeout global augmenté, aucun retry augmenté. |
| 2026-08-02 | 2.8 | Clôture Phase 5D et Phase 5 — CTA final `#contact` + retrait de `/design-preview`. `HomeCallToAction.vue` (huitième section, ancre `#contact`, eyebrow `Parlons de votre projet`, H2 `Construisons une présence digitale à la hauteur de votre entreprise.`, paragraphe explicatif). Stratégie de contact conditionnelle (DEC-038) : mailto `BaseButton` si `site.contact.email` défini, sinon aucun bouton ni lien. Notice preprod (DEC-039) publiée uniquement si `!contact.email && useRuntimeConfig().public.siteIndexable === false` (DEC-040 : jamais `process.env`). `pages/index.vue` orchestre 8 sections dans l'ordre édito `docs/01-CONTENT.md §7.1`. `navigation.ts` : `primaryCta.to = "/#contact"` (source unique header desktop/footer/menu mobile) ; header nav « Expertises » recablé vers `/#expertises`. Suppression de `apps/web/app/pages/design-preview.vue` et `test/e2e/design-preview.spec.ts` (DEC-041) ; coverage utile migrée dans `test/e2e/home-structure.spec.ts` ; `test/e2e/seo.spec.ts` vérifie que `/design-preview` renvoie 404 ; sitemap : plus d'`exclude` (une seule route `/`). Tests : 130 unitaires (+10 `HomeCallToAction.spec.ts` avec `vi.mock('~/config/site')` pilotant email null/set × indexable true/false) et suites E2E `home-cta-final.spec.ts` (13) + `home-structure.spec.ts` (5) ; `home-sections-secondary.spec.ts` étendu à 8 sections ; `beforeEach` de 5 suites clavier/mobile/reduced-motion/responsive/accessibility migrés vers `/`. `npm run quality` vert (lint, typecheck, tests 130/130, build). Phase 5 close entièrement : accueil one-pager complet, aucune page interne clonée, aucun placeholder, aucune coordonnée fictive. |
| 2026-08-02 | 2.12 | Phase 6C — Configuration OVHcloud, Turnstile facultatif et 503 sur échec SMTP. Nouveau sous-domaine backend `App\Contact\Configuration\` : `ContactConfigurationIssue` (VO readonly `error`/`warning`), `ContactConfigurationReport` (liste d'issues + `isValid()`), `ContactConfigurationValidator` (service pur sans I/O — 8 règles : `MAILER_DSN` non vide + non `null://null` en prod + non plaintext en prod, `CONTACT_RECIPIENT` présent + valide, `CONTACT_FROM_EMAIL` présent + valide + non-example en prod (warning), `CONTACT_FROM_NAME` non vide, Turnstile activé ⇒ secret présent, Turnstile désactivé en prod ⇒ warning, `CONTACT_ORIGIN_ALLOWLIST` non vide, `TRUSTED_PROXIES` warning si vide ; la sortie ne divulgue **jamais** le DSN complet, le secret Turnstile ni les emails en clair). Commande CLI `bin/console app:contact:check` (`#[AsCommand]`, SymfonyStyle, exit `0` succès / `1` erreurs bloquantes). Marker de domaine `ContactTemporarilyUnavailableException`. `SymfonyContactMessageSender` capture `TransportExceptionInterface` et re-lève l'exception marker (chaîne l'origine pour debug interne). `CONTACT_RECIPIENT` absent en prod est traité identiquement (503 avant même l'envoi). Nouveau `ContactSubmissionResult::STATUS_TEMPORARY_ERROR` → HTTP 503 + `{"status":"error","code":"temporary_error","request_id":"…"}` + `Cache-Control: no-store` + `X-Request-Id`. Log `contact.mailer_unavailable` (canal `contact`, niveau warning, aucun PII — mêmes garanties que le happy path). `services.yaml` : params `app.mailer.dsn`, `app.trusted_proxies`, service `ContactConfigurationValidator` wiré. `InMemoryContactMessageSender` étendu (`failNextWith`/`failNextTemporarily` — seam explicite pour simuler l'échec SMTP en tests sans réseau). **Front** : `NUXT_PUBLIC_TURNSTILE_ENABLED` (défaut sûr `false`) ajouté à `runtimeConfig.public`, `nuxt.config.ts`, `.env.example`, `compose.yaml`. `TurnstileWidget.vue` gagne la prop `enabled` (dev-noop si `!enabled || !siteKey`) — miroir des deux flags DEC-056. `ContactForm.vue` gagne la prop `turnstileEnabled` (transmise depuis `HomeCallToAction.vue` qui lit `runtimeConfig.public.turnstileEnabled`) et bandeau verbatim « Service momentanément indisponible » / « Le service est momentanément indisponible. Votre message n'a pas été envoyé. Merci de réessayer plus tard. » sur code `temporary_error`. `useContactForm` mappe HTTP 503 → `temporary_error` (fallback `if httpStatus === 503`) ; `types/contact.ts` ajoute `'temporary_error'` à `ContactErrorCode`. **Tests** : PHPUnit +17 (14 `ContactConfigurationValidatorTest` + 3 `ContactCheckCommandTest` pure `TestCase` — hermétique, sans KernelTestCase, résout le problème d'inlining du service au container : validateur instancié directement avec paramètres hardcodés pour couvrir tous les scénarios sans dépendance runtime ; +1 `SymfonyContactMessageSenderTest::testSendConvertsTransportException…` via classe anonyme implémentant `TransportExceptionInterface` ; +1 `ContactSubmissionControllerTest::testMailerFailureReturns503…` via `failNextTemporarily` ; +1 `ContactLoggingTest::testMailerFailureLogsWarningWithoutPii`). Vitest +2 (`useContactForm.spec.ts` : HTTP 503 → `temporary_error` + valeurs (email/message/turnstileToken) préservées ; `ContactForm.spec.ts` describe « global error banner on HTTP 503 » : bandeau verbatim + valeurs préservées). Playwright +2 dans `contact-form.spec.ts` : (1) 503 `temporary_error` → bandeau verbatim + `request_id` + valeurs préservées ; (2) Turnstile désactivé (défaut) → 0 requête vers `challenges.cloudflare.com`, 0 `<script src*="challenges.cloudflare.com">`, notice dev visible dans le DOM. **Docs** : nouveau ADR-008 (`docs/adr/ADR-008-mailer-ovhcloud-turnstile-optionnel.md` — Contexte / Options / Décision transport SMTP + Turnstile deux flags + 503 verbatim / Validateur / Raisons / Conséquences / Références) ; nouvelle checklist opérationnelle `docs/checklists/PRODUCTION-CONTACT.md` (10 sections : prérequis OVHcloud, variables d'env, alignement Turnstile, pré-déploiement `app:contact:check`, test réel maîtrisé unique préfixé `[TEST-PROD]`, observabilité, dégradation contrôlée avec restauration, robustesse, revue sécurité rapide, docs, rollback) ; `docs/adr/README.md` ajoute l'index des ADR acceptés ; `docs/05-SECURITY-PRIVACY.md` §14.9 étendue « Transport mail et échec SMTP (Phase 6C) » ; `docs/06-ARCHITECTURE-CODE.md` §16.2 (arborescence `apps/api/src/Contact/{…,Configuration,Command,Exception}`) et §16.10 (ADR-008 accepté, ADR persistance renuméroté vers un futur ADR dédié) ; `docs/07-QUALITY-DELIVERY.md` §20 ajoute « Diagnostic pré-déploiement (Phase 6C) » ; `docs/08-ROADMAP.md` : jalon 6C TERMINÉE + critère de sortie Phase 6 étendu au chemin d'échec ; `README.md` racine et `apps/api/README.md` + `apps/web/README.md` mis à jour (nouvelle variable `NUXT_PUBLIC_TURNSTILE_ENABLED`, commande `app:contact:check`, sous-domaines `Configuration/`, `Command/`, `Exception/`). Point d'infrastructure : aucune connexion SMTP réelle en CI ni en tests ; toute la chaîne est vérifiée hermétiquement via seams (`InMemoryContactMessageSender::failNextTemporarily`, `page.route('**/api/contact')`, `ContactConfigurationValidator` pur). |
| 2026-08-02 | 2.13 | Phase 7A — Architecture éditoriale et pages institutionnelles `/agence` + `/expertises`. Nouveau contrat typé `app/config/expertise-pages.ts` (`ExpertisePage[]`, 5 entrées `status: "planned"`, chacune avec `id`/`pillarId`/`slug`/`route`/`title`/`shortTitle`/`summary`/`services[3]`) — source unique de vérité pour le routage futur des 5 pages `/expertises/{slug}` (Phase 7B), séparée de `expertise-pillars.ts` (contenu narratif partagé — cf. DEC-058). 4 composants éditoriaux réutilisables dans `app/components/editorial/` et `app/components/expertise/` : `EditorialHero.vue` (eyebrow + H1 + lead + slot `actions`, `<section aria-labelledby>` via `useId()`), `EditorialSection.vue` (H2 obligatoire + eyebrow/intro optionnels + slot par défaut, variantes `default`/`subtle`/`inverse`, prop `sectionId` pour ancre facultative), `EditorialCallout.vue` (bandeau CTA H2 + primaryCta obligatoire + secondaryCta facultatif, variantes `inverse`/`accent`, contrat `CalloutLink = {label, to, external?}`), `ExpertiseOverviewCard.vue` (`<article data-variant>`, H3 non lié — DEC-060, description longue verbatim, liste `<ul>` de 3 services, badge d'ordre zéro-paddé `aria-hidden`, **aucun `<a>`, aucun `tabindex` en 7A**). 2 pages orchestrées `pages/agence.vue` (H1 verbatim « Une agence digitale à taille humaine, pensée pour accompagner les entreprises dans leur globalité. », eyebrow `L'agence`, 3 `EditorialSection` — Positionnement / Fonctionnement / Valeurs, callout final vers `/expertises`, `usePageSeo({title: "Agence digitale à taille humaine", …})`) et `pages/expertises/index.vue` (H1 verbatim « Cinq pôles complémentaires pour construire une présence digitale cohérente. », eyebrow `Nos expertises`, `EditorialSection` approche, grille de 5 `ExpertiseOverviewCard` alimentée par `expertisePillars` — jamais une source parallèle, callout final vers `/agence`, `usePageSeo({title: "Expertises web, design, contenu et SEO", …})`). Pré-rendu étendu (`nuxt.config.ts::routeRules['/agence'|'/expertises'] = { prerender: true }`) — 3 pages publiques pré-rendues au build (`/`, `/agence`, `/expertises`), vérifiées sur `.output/public/{agence,expertises}/index.html`. **Navigation refondue** : `navigation.ts` gagne un champ `isRoute: boolean` par entrée (cf. DEC-059) pilotant `NuxtLink :external="item.isRoute ? undefined : true"` — remplace la prop `external` codée en dur du header/footer/mobile-nav, met fin aux warnings vue-router R0004 sur les ancres. `primaryNavigation` = Expertises (`/expertises`, isRoute) + Agence (`/agence`, isRoute) + Réalisations (`/#realisations`, ancre) ; liens morts `Méthode` et `Ressources` supprimés. `footerNavigation` réduit au seul groupe `Découvrir` (3 entrées vivantes) ; `legalNavigation = []` (aucune page légale publiée, `v-if="legalNavigation.length > 0"` sur le bloc footer). `SiteHeader.vue`, `SiteFooter.vue`, `MobileNavigation.vue` mis à jour pour lire `item.isRoute`. **Tests** : 213/213 unitaires verts (+51 sur 6C 162) — 11 `expertise-pages.spec.ts` (count=5, unique ids/slugs, mapping bijectif vers `expertisePillars`, slug pattern, `status === "planned"` pour toutes, services miroir des pillars, ordre préservé), 7 `EditorialHero.spec.ts`, 9 `EditorialSection.spec.ts` (chaque variante `data-tone`, `aria-labelledby` généré, `sectionId` propagé), 7 `EditorialCallout.spec.ts`, 6 `ExpertiseOverviewCard.spec.ts` (« no focusable element » verrouille l'absence d'interactivité 7A — DEC-060), 6 `pages/agence.spec.ts` (H1 verbatim, eyebrow, intro, SEO input `usePageSeo`, aucun lien vers `/expertises/{slug}` / `/methode` / `/realisations` / `/ressources`, aucune donnée fictive `lorem`/`john doe`/`@example`/`fondée en \d{4}`/`plus de \d+ (clients\|projets\|années)`), 7 `pages/expertises.spec.ts` (H1 verbatim, eyebrow, intro, SEO, 5 cartes présentes, aucun lien vers `/expertises/[a-z]`, aucune donnée fictive). Nouvelle suite E2E `test/e2e/institutional-pages.spec.ts` (18 cas, portée sur `/agence` + `/expertises` + maillage) : renvoie 200 avec un H1 unique verbatim par page (aucune erreur console), publie eyebrow et introduction verbatim en SSR (fetch `request.get()` avant hydratation), expose SEO complet dans le HTML initial (title spécifique, meta description avec extrait verbatim, canonical **ou** noindex — respect politique globale, `og:url` absolu), propose lien réel vers page sœur + `/#contact`, ne contient aucun `a[href^="/expertises/"]` (Phase 7B), affiche 5 `.expertise-overview-card` sur `/expertises` sans aucun `<a>` interne, responsive 390 / 768 / 1440 sans débordement horizontal, Axe WCAG 2.2 AA sans violation `serious`/`critical` sur chaque page, `prefers-reduced-motion` (H1 visible), et 3 tests inter-pages (callout `/agence` → `/expertises` par clic, callout `/expertises` → `/agence` par clic, navigation principale expose Agence + Expertises + Réalisations + CTA `/#contact`). Build : 2.92 MB brut / 759 kB gzip (+70 KB / +18 KB vs 6C 2.85 MB / 741 kB) — les 4 composants éditoriaux et les 2 pages tiennent dans le budget attendu. Aucune dépendance ajoutée, TypeScript strict conservé, aucune donnée inventée (chiffre, client, adresse, membre d'équipe), aucun lien mort dans la navigation. Sections différées explicites : les 5 pages `/expertises/{slug}` (7B — contenu à valider avec le client), le remplacement de `HomeFeaturedCaseStudy` par de vraies études de cas (7C). |
| 2026-08-03 | 2.13 | Phase 7A — Architecture éditoriale et pages institutionnelles `/agence` + `/expertises`. Config typée `app/config/expertise-pages.ts` (5 pages `status: "planned"`, aucune route générée — cf. DEC-058). 4 composants éditoriaux réutilisables : `EditorialHero.vue` (eyebrow + H1 + lead + slot actions, `aria-labelledby` via `useId()` — cf. DEC-061), `EditorialSection.vue` (H2 obligatoire, variantes `default`/`subtle`/`inverse` via `data-tone`), `EditorialCallout.vue` (bandeau CTA H2 + primaryCta + secondaryCta facultatif, variantes `inverse`/`accent`, `NuxtLink :external` conditionnel selon `to`), `ExpertiseOverviewCard.vue` (`<article>`, H3 non lié — cf. DEC-059, 3 services `<ul>`, badge d'ordre zero-padded `aria-hidden`). 2 pages : `pages/agence.vue` (H1 verbatim « Une agence digitale à taille humaine, pensée pour accompagner les entreprises dans leur globalité. », eyebrow `L'agence`, intro verbatim, 3 sections Positionnement/Fonctionnement/Valeurs, callout final vers `/expertises`) et `pages/expertises/index.vue` (H1 verbatim « Cinq pôles complémentaires pour construire une présence digitale cohérente. », eyebrow `Nos expertises`, intro verbatim, section approche, grille de 5 `ExpertiseOverviewCard` alimentée par `expertisePillars`, callout final vers `/agence`). `usePageSeo` sur les 2 pages (titles `Agence digitale à taille humaine` et `Expertises web, design, contenu et SEO`, canonicals absolus, `type: "website"`). `routeRules` étendu : `/agence` et `/expertises` pré-rendus au build. Navigation refondue : `NavigationItem` gagne un champ `isRoute: boolean` (cf. DEC-060), `primaryNavigation` = Expertises `/expertises` + Agence `/agence` + Réalisations `/#realisations` (liens morts `Méthode`, `Ressources` supprimés), CTA `Parler de votre projet` → `/#contact`, footer réduit au groupe `Découvrir` (3 entrées vivantes), `legalNavigation = []` (aucune page légale publiée). Trois composants layout mis à jour (`SiteHeader.vue`, `SiteFooter.vue`, `MobileNavigation.vue`) : `NuxtLink :external="item.isRoute ? undefined : true"` → fin des warnings `[VUE_ROUTER_R0004]`. **Tests** : 213/213 unitaires verts (+51 : `expertise-pages` ×11, `EditorialHero` ×7, `EditorialSection` ×9, `EditorialCallout` ×7, `ExpertiseOverviewCard` ×6, `/agence` ×6, `/expertises` ×7). Nouvelle suite E2E `test/e2e/institutional-pages.spec.ts` (18 cas répartis en 3 describe : SSR + contenu + SEO `/agence`, id. `/expertises`, maillage inter-pages — SSR HTML, unique H1 verbatim, eyebrow + intro en SSR, SEO complet (title, description, canonical *ou* noindex, `og:url`), lien réel `/#contact`, absence de lien vers `/expertises/{slug}`, 5 cartes sans `<a>`, navigation principale exposant Agence + Expertises + Réalisations + CTA `/#contact`, maillage `/agence` ↔ `/expertises` réellement navigable, responsive 390/768/1440, Axe WCAG 2.2 AA sans violation `serious`/`critical`, `prefers-reduced-motion` — H1 visible). Curl SSR vérifié sur `.output/public/{agence,expertises}/index.html` : titres et canonicals corrects, H1 uniques, eyebrows et intros verbatim présents avant hydratation. Build : 2.92 MB / 759 kB gzip (+70 KB / +18 KB vs 6C 2.85 MB / 741 kB). `npm run lint` + `npm run typecheck` + `npm run test` + `npm run build` tous verts. Non-goals confirmés Phase 7A : aucune route `/expertises/{slug}` (Phase 7B), aucune page `/realisations` (Phase 7C), aucun contenu client fictif, aucun chiffre inventé. |
| 2026-08-03 | 2.14 | Phase 7B — Livraison des 5 pages d'expertise détaillées `/expertises/{concevoir,construire,valoriser,visibilite,faire-evoluer}`. Contrat `app/config/expertise-pages.ts` étendu de 11 champs `readonly` (`eyebrow`, `introduction`, `seoTitle`, `seoDescription`, `needTitle`, `needDescription`, `approachTitle`, `approachDescription`, `deliverables[]`, `benefits: ExpertiseBenefit[]`, `relatedPillarIds[]`) + nouveau type `ExpertiseBenefit`. Les 5 entrées passent `status: "planned"` → `"published"` avec contenu éditorial verbatim par pôle. Route dynamique **unique** `app/pages/expertises/[slug].vue` (cf. DEC-064) : résolution slug via `expertisePages.find(p => p.slug === slug && p.status === "published")`, `createError({ statusCode: 404, fatal: true })` sur slug inconnu (jamais de redirection ni de rendu vide), orchestre `SiteBreadcrumb` + `ExpertisePageHero` + 4 `EditorialSection` (À qui cela s'adresse / Notre approche / Livrables / Bénéfices) + `ExpertiseRelatedPillars` + `EditorialCallout` de retour. Nouveau composable `app/composables/useExpertiseServiceSchema.ts` (cf. DEC-065) : JSON-LD `@type: Service`, `@id: ${url}#service`, `provider: { "@id": ${origin}/#organization }` (référence l'`@id` de l'Organization émise par `useSiteSchema` au niveau du layout — jamais de duplication de champs), `serviceType: shortTitle`. Aucun `offers`/`price`/`aggregateRating`/`review` (règle 1 AGENTS.md — rien inventer). 5 nouveaux composants : `SiteBreadcrumb.vue` (`<nav aria-label="Fil d'Ariane">`, `<ol role="list">`, séparateur `›` `aria-hidden`, dernier item `aria-current="page"`), `ExpertisePageHero.vue` (H1 unique + eyebrow + introduction), `ExpertiseDeliverables.vue` (`<ul role="list">` de pastilles + marqueurs `aria-hidden`), `ExpertiseBenefits.vue` (grille H3+description), `ExpertiseRelatedPillars.vue` (2 cartes-liens vers pôles connexes, filtrage `status === "published"` — dégradation silencieuse, cf. DEC-066). `ExpertiseOverviewCard.vue` refactorée en carte cliquable conditionnelle : `<NuxtLink v-if="isPublished()">` + `<article v-else>` avec contenu dupliqué (**pas** `<component :is>` qui laisserait un `<nuxtlink>` custom element non résolu à l'hydratation, cf. DEC-062). CTA `Découvrir ce pôle →` en `--color-petrol` (contraste WCAG AA ≥ 6:1 sur cream/sand, vs 3.19:1 avec `--color-devzair-blue` — cf. DEC-063), font-weight 700. `pages/expertises/index.vue` joint `expertisePillars` × `expertisePages` via `pagesByPillarId` (Map computed) — passe `:page="pagesByPillarId.get(pillar.id)"` à chaque carte. `nuxt.config.ts routeRules` étendu : 5 nouvelles routes pré-rendues (`/expertises/{concevoir,construire,valoriser,visibilite,faire-evoluer}`), vérifiées sur `.output/public/expertises/{slug}/index.html`. Sitemap XML mis à jour automatiquement (chaînage `@nuxtjs/sitemap` sur `routeRules.prerender`), aucune configuration additionnelle. Test E2E `institutional-pages.spec.ts` réaligné : l'assertion « ne rend aucune carte comme lien tant que Phase 7B n'est pas livrée » (Phase 7A) devient « 5 cartes exposent chacune un lien vers `/expertises/{slug}` » (Phase 7B). **Tests** : 258/258 unitaires verts (+45 sur 213 Phase 7A) — `expertise-pages` étendu (+9 assertions groupées : 11 champs requis, `status === "published"` universel, 2 `relatedPillarIds` par entrée exactement, pas d'auto-référence, cible existante et publiée, deliverables non vide, benefits non vide, seoTitle sous 60c…), `SiteBreadcrumb` ×6, `ExpertisePageHero` ×5, `ExpertiseDeliverables` ×4, `ExpertiseBenefits` ×5, `ExpertiseRelatedPillars` ×5, `useExpertiseServiceSchema` ×5, `pages/expertise-slug` ×7 (mount dynamique par slug via `vi.resetModules()` + `await import`, garde-fou createError 404, garde-fou usePageSeo appelé une seule fois par page, garde-fou useExpertiseServiceSchema appelé une seule fois, garde-fou fil d'Ariane 3 items dernier `aria-current="page"`). Nouvelle suite E2E `test/e2e/expertise-pages.spec.ts` : SSR 200 par slug (contenu H1 + eyebrow + introduction verbatim), HTTP 404 sur slug inconnu (`/expertises/slug-inexistant-42`), sitemap XML contient les 5 routes, JSON-LD `@type":"Service"` présent dans le HTML pré-rendu de chaque page, fil d'Ariane accessible, absence de placeholder/lorem/TODO, 2 liens de pôles connexes navigables, mobile-nav opérationnelle sur la page slug, responsive 320/390/768/1024/1440 sans débordement horizontal, Axe WCAG 2.2 AA sans violation `serious`/`critical`, `prefers-reduced-motion` respecté. Régex tolérante à l'encodage HTML des apostrophes SSR (`'` ⇔ `&#39;` ⇔ `&#x27;`) dans les assertions body/title. **Playwright complet : 200/200 verts, 0 flaky, 0 failed** (`--workers=1 --retries=0`, image `mcr.microsoft.com/playwright:v1.62.1-noble`, 1.2 min). Build : 3.00 MB brut / 782 kB gzip (+80 KB / +23 KB vs Phase 7A 2.92 MB / 759 kB) — **8 pages publiques pré-rendues** (`/`, `/agence`, `/expertises`, 5 × `/expertises/{slug}`). Lint, typecheck, unit tests, build, Playwright tous verts. Aucune dépendance ajoutée. TypeScript strict conservé. Aucune donnée fictive (chiffre, client, adresse, membre d'équipe, certification, prix, avis). Non-goals confirmés Phase 7B : aucune page `/realisations/{slug}` (Phase 7C), aucune persistance PostgreSQL (Phase 8), aucune modification du formulaire de contact (Phase 6 close). |
| 2026-08-02 | 2.11 | Phase 6B — Formulaire de contact accessible + widget Turnstile + intégration dans la section `#contact` de l'accueil. Nouveau domaine `app/components/contact/` : `ContactForm.vue` orchestrateur (`<form novalidate aria-labelledby aria-describedby>`, fieldset radiogroup `projectType`, honeypot `website` hors flux + `aria-hidden` + `tabindex=-1`, notice RGPD référencée par `aria-describedby`, focus management post-submit — bandeau `role="status"` en succès / `role="alert"` en erreur, focus au premier champ invalide), `ContactFormField.vue` (label + input/textarea + `aria-invalid` + `aria-describedby` d'erreur), `ContactFormStatus.vue` (bandeau contextuel accessible), `TurnstileWidget.vue` client-only avec fallback dev-noop lorsque `NUXT_PUBLIC_TURNSTILE_SITE_KEY` est vide (DEC-053, miroir de `AlwaysAllowTurnstileVerifier` — DEC-047). Composable `useContactForm` pur (DEC-052) : `endpoint` et `fetcher?` injectés, aucune dépendance à `useFetch` / `useRuntimeConfig` ; validation client miroir non strict du DTO Symfony (`ContactRequest`) ; mapping HTTP 200/202/400/403/413/429 → codes ADR-007 avec `Retry-After` parsé pour le 429 ; protection double-clic (submit désactivé pendant `isSubmitting`) ; reset des champs sur succès. Type domaine partagé `app/types/contact.ts`. `HomeCallToAction.vue` : suppression du mailto conditionnel et de la notice preprod (placeholders Phase 5D), remplacés par `<ContactForm />` qui devient la CTA du one-pager. `runtimeConfig.public.turnstileSiteKey` ajouté (`nuxt.config.ts`, `.env.example`, `compose.yaml`) avec défaut vide → mode dev-noop. Honeypot serveur silencieux verrouillé côté UI (DEC-054) : réponse 202 générique = même bandeau succès qu'un envoi réel, jamais de différenciation visuelle. Tests unitaires : 154/154 verts (+24) — 20 `useContactForm.spec.ts` (validation par champ, mapping HTTP par code, Retry-After, double-submit, reset succès) + 4 `ContactForm.spec.ts` (structure SSR, honeypot hors flux, ARIA, focus). Tests E2E : 117/117 verts (+7) — nouvelle suite `contact-form.spec.ts` (8 tests : SSR du form, happy path dev-noop → 200 accepted, HTTP 400 par champ + `aria-invalid`, honeypot rempli + 202 → succès UI inchangé, HTTP 429 + `Retry-After: 42` → bandeau global, validation client bloque une soumission vide sans `fetch`, Axe WCAG 2.2 AA restreint au form, `prefers-reduced-motion` — backend mocké via `page.route('**/api/contact', …)` : suite indépendante de la stack Symfony) + `home-cta-final.spec.ts` réaligné (Phase 5D → 6B : test « shows the preprod notice » supprimé, nouveau test `hosts a real <form> with name, email, message and consent fields`, assertions mailto/tel/href="#" conservées avec périmètre `#contact`, contrôle « no internal implementation details » élargi à `!phase 6`, `!todo`, `!lorem ipsum`). PHPUnit backend intact : 31/31 verts. Build : 2.85 MB / 740 kB gzip (+50 KB / +12 KB gzip vs 2.10, dans le budget +80 KB / +20 KB). `npm run quality:full` vert (lint, typecheck, 154 unitaires, build, 117 E2E — 0 flaky, 0 failed, 53.4 s). Point d'infra confirmé : sous Alpine musl, Playwright doit tourner depuis l'hôte (Ubuntu, chromium en cache) en pointant `PLAYWRIGHT_BASE_URL=http://localhost:3001` (Caddy). Prochaine étape (Phase 12 ou déclencheur produit) : brancher une site-key Turnstile réelle + `MAILER_DSN` production, capturer les 429 réels via le canal Monolog `contact` pour ajuster le token bucket. |

---

---

## Règle de maintenance

Ce fichier doit être modifié uniquement lorsque les règles de son domaine évoluent.  
Les tâches réalisées, décisions et blocages doivent être consignés dans `10-TRACKING.md`.
